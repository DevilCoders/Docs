Библиотека, которая является оберткой протобуфных классов, которые передаются через `ecstatic` от сервиса `mtinfo` к сервису `predictor`.<br>
Главная задача – уметь парсить класс `Forecasts` из `std::string` и уметь сериализовывать данный класс в `std::string`.

# Tile cache generator a.k.a. cachegen
Generates raster&vector tile caches of map layers in pbstream/fb formats for mapkit-based apps and legacy navigator. 

This tool if a part of of tile_cache_builder module for garden.


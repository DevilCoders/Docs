# Build
Use ```make install_deps@[darwin/linux]``` and ```make app@[darwin/linux]``` to build for the first time or rebuild after svn up
Use ```make app@[darwin/linux]``` to rebuild debugger after changes in mapkit and ```make app_only@[darwin/linux]``` to rebuild only app after changes in debugger

# Run
```./guidance_debugger```

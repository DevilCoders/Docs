The tool replaces target property from address to geocoder id.

## Usage

With this tool we can encode filter_points_table 
from schema.FILTERED_POINTS_SCHEMA_V1 format
to schema.FILTERED_POINTS_SCHEMA_V2 format.

```./geocoder_address --source {source_table_path} --destination {destination_table_path}```
# error

Отображает ошибку.
Используется в `server/controllers/util` в методах `renderNoAccessPage()` и `renderErrorPage()`.

```js
{
    block: 'error',
    content: 'Алярма! Случилось страшное!'
}
```

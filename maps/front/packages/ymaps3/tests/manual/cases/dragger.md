<!--
Title:
Базовая проверка контролов зума

Description:
Проверка контролов зума.
Памятка по терминам: https://wiki.yandex-team.ru/eva/testing/Projects/maps-api/

Components: 
smoke

Estimated time: 
300000

Precondition:
Перейти на тестовый стенд: https://front-jsapi.crowdtest.maps.yandex.ru/3.0/stand/index.html
При загрузке стенда появляется карта со спаном Москвы, слева имеется панель тестовых контролов, в центре карты зеленый маркер с координатами.

Step:
  Action:
  Выполнить клик в кнопку "vector". 

  Expectation:
  Карта в контейнере обновилась, маркер с координатами пропадает.

Step:
  Action:
  Выполнить клик в кнопку "Marker drag". 

  Expectation:
  Карта загружается в контейнере заново, при этом в центре спана маркер - зеленая прямоугольная область с координатами точки, вида "37.588 55.7338".

Step:
  Action:
  Навести курсор на маркерную метку, зажав ЛКМ перенести метку на спане карты.  
  
  Expectation:
  При зажатии ЛКМ метка прилипает к курсору, движется вместе в курсором.
  При переносе маркер становится желтого цвета, внутри меняются координаты.
  После установки маркер снова зеленый, координаты точки на месте.

Step:
  Action:
  Навести курсор на маркерную метку с "Пиццей", зажав ЛКМ подергать метку вправо-влево, вверх-вниз, затем переместить ее в другое(относительно первоначального) место на карте.  
  
  Expectation:
  При зажатии ЛКМ метка прилипает к курсору, движется вместе в курсором.
  При переносе маркер становится желтого цвета, внутри меняются координаты.
  После установки маркер снова зеленый, координаты точки на месте.

Step:
  Action:
  Зажав ЛКМ дёрнули карту (не метку) немного вправо-влево, вверх вниз(статичный драг) несколько раз.
  
  Expectation:
  Перемещение карты драгом корректное: нет странных визуальных эффектов на карте. 
  Карта не дрожит и не мигает цветами.
  Нет заметных тормозов при передвижении карты.
  Метка-маркер при драге остается в центре карты. При этом новые координаты в маркере не появляются.

Step:
  Action:
  Выполнить инертный драг: зажав ЛКМ дернули карту (не метку) с усилием и отпустили ЛКМ, так, что спан карты продолжит движение до остановки.
  
  Expectation:
  Перемещение карты драгом корректное: нет странных визуальных эффектов на карте. 
  Карта не дрожит и не мигает цветами.
  Нет заметных тормозов при передвижении карты.
  Метка-маркер при драге остается в центре карты. При этом новые координаты в маркере не появляются.

Step:
  Action:
  Выполнить статичный и инертный драг карты(не метки) на тачпаде несколько раз.
  
  Expectation:
  Перемещение карты драгом корректное: нет странных визуальных эффектов на карте. 
  Карта не дрожит и не мигает цветами.
  Нет заметных тормозов при передвижении карты.
  Метка-маркер при драге остается в центре карты. При этом новые координаты в маркере не появляются.

Step:
  Action:
  Тачем на тачпаде навести курсор на метку-маркер, захватить тачпадом метку подергать метку вправо-влево, вверх-вниз, затем переместить ее в другое(относительно первоначального) место на карте.  
  
  Expectation:
  При захвате тачпадом метка прилипает к курсору, движется вместе в курсором.
  При переносе маркер становится желтого цвета, внутри меняются координаты.
  После установки маркер снова зеленый, координаты точки на месте.
  
Step:
  Action:
  Скролом мыши призумится/отзумится на карте. 
  
  Expectation:
  Метка маркер при зуме/отзуме не смещается на спане карты - остается на месте, координаты в метке не меняются.
-->
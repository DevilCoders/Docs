# maps._staff

## Types

If you want update types, you should change schemas and run:

```sh
npx lerna run generate-types --scope=@yandex-int/maps._staff
```

All schemas you can find in [documentation](https://staff-api.yandex-team.ru/v3/).

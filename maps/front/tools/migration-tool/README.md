# Миграция из /quarantine

`npm run migrate <repo-name>` (`repo-name` должен быть на верхнем уровне в `/quarantine`).

Инструкция и последовательность действий: https://wiki.yandex-team.ru/users/alexpavlovich/dobro-pozhalovat-v-arkadiju/

Руками нужно будеть поправить `ci.js` (при необходимости), `a.yaml` и ссылки в ABC.

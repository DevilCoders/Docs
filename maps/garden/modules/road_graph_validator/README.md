# road_graph_validator

## Module description
This module validates road graph. It compares routes built in datavalidation
and stable routers, and starts graph_deployment if the comparison is successful.

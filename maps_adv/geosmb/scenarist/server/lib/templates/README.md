# Список шаблонов и их параметров

### template_for_testing
Plain-text-шаблон для тестиования

_Параметры_:

`name` - просто в теле письма

`another_name` - просто в теле письма (с текстом по умолчанию "значение не передали")

### lost_advert
_Параметры_:

`org_name` - название организации (в title и в теле письма)

`button_url` - ссылка на кнопке

### lost_several
_Параметры_:

`button_url` - ссылка на кнопке

### lost_no_advert
_Параметры_:

`button_url` - ссылка на кнопке

### loyal_no_advert_less_month
_Параметры_:

`button_url` - ссылка на кнопке

### loyal_no_advert_more_month
_Параметры_:

`button_url` - ссылка на кнопке

### disloyal_no_advert_less_month
_Параметры_:

`button_url` - ссылка на кнопке

### disloyal_no_advert_more_month
_Параметры_:

`button_url` - ссылка на кнопке


### loyal_advert_less_month
_Параметры_:

`org_name` - название организации (в теле письма)

`button_url` - ссылка на кнопке


### loyal_advert_more_month
_Параметры_:

`org_name` - название организации (в теле письма)

`button_url` - ссылка на кнопке

### disloyal_advert_less_month
_Параметры_:

`org_name` - название организации (в теле письма)

`button_url` - ссылка на кнопке

### disloyal_advert_more_month
_Параметры_:

`org_name` - название организации (в теле письма)

`button_url` - ссылка на кнопке


### potential_advert
_Параметры_:

`org_name` - название организации (в теле письма)

`button_url` - ссылка на кнопке

### potential_no_advert
_Параметры_:

`button_url` - ссылка на кнопке

### potential_several
_Параметры_:

`button_url` - ссылка на кнопке

### loyal_several_less_month
_Параметры_:

`button_url` - ссылка на кнопке

### loyal_several_more_month
_Параметры_:

`button_url` - ссылка на кнопке

### disloyal_several_less_month
_Параметры_:

`button_url` - ссылка на кнопке

### disloyal_several_more_month
_Параметры_:

`button_url` - ссылка на кнопке

# whosfat_column

Иногда в таблице лежит очень тяжелая колонка, которая замедляет чтение таблицы или операции над ней. Хочется найти её и починить!

Схема таблицы не важна.

## Использование

```
./whosfat_column \
    --yt-proxy arnold \
    --yt-input-table //home/market/testing/indexer/users/20210924_1028/genlog/0000 \
```

## Параметры командной строки

```./whosfat_column --help```

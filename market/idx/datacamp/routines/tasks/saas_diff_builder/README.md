## Построениие таблицы с оферами из хранилища, которых нет в saas

Используется для доливки отсутствующих оферов/промо в saas.

```
Usage: ./tasks_runner saas_diff [OPTIONS] [ARG]...

Required parameters:
  --yt-proxy VAL     
  --config-path VAL  
  --output VAL       

```

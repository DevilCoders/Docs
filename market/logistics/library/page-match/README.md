**РageMatchController**

Для использования добавить проперти `page-match.api-name` с названием апи, так же сделать импорт конфигурации `PageMatchConfig`.

Если используется spring версии больше 5.3, требует указания настройки `spring.mvc.pathmatch.matching-strategy=ant_path_matcher` в `application.properties`.

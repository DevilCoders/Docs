Парсер репорта для нужд байбокса. Для каждой msku из входной таблицы ходит в place product_offers и парсит BUYBOX_DEBUG_LOG.

Пример использования есть в папке integral_tests/test.sh, пример входных данных в data/input.tsv

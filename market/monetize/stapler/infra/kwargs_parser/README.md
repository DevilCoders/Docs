# kwargs_parser

## cli
```
Usage: kwargs_parser [OPTIONS] NAME VALUE OUTPUT

Options:
  --help  Show this message and exit.
```

## nirvana job-command
```
bash -c '${resource["kwargs_parser"]} ${param["arg_name"]} ${input["arg_value"]} ${output["key_value"]}'
```

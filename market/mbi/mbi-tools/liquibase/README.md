# Инструмент для работы с liquibase

Скрипт для локального поднятия ликвибейза.

Чтобы использовать скрипт поместите `startLiquibase-template.sh` 
в папку с вашим проектом, заменив <> на нужные вам параметры.

Пример работы со скриптом можно посмотреть в 
`market/mbi/transfer-permissions-storage`
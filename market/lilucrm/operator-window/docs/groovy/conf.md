# Работа с конфигурацией системы

Для получения значений конфигурации, в т.ч. фиче-флагов, существует API `api.conf`.
 
Пример получения значения конфигурации
```groovy
def value = api.conf.paramCode
```
,
где paramCode - код параметра конфигурации.
# swagger-diff

В данной папке лежит обёртка над библиотекой swagger-diff.

Обёртка предоставляет функцию, получающую две swagger-схемы и список имён правил,
которые нужно исключить из проверок. Функция возвращает промис, который будет
разрешён с результатами проверок известных применённых правил.

# Микроформат пакета с офферами

Набор офферов, сгруппированных по некоторому признаку,
называется __пакетом__ (_паком_) с офферами.

Например:
1) Плейс `alt_same_shop` ищет паки с офферами из одного магазина.
2) Плейс `alt_fast_delivery` ищет паки с офферами с быстрой доставкой.

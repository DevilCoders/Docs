# Виджет синих интентов

Копия `@self/root/src/widgets/content/search/Intents`

*Но работает с белым `visibleSearchResult`*

Используется на белой выдаче. При этом на белых страницах special используется `@self/root/src/widgets/content/search/Intents`, использующий синий `visibleSearchResult`.

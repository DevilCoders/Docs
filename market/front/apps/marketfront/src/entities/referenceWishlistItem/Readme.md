# ReferenceWishlistItem

Представляет собой маппинг идентификатора `ReferenceId`, соответствующего сущности `Reference`
(в данный момент поддерживается только `Sku`), с элементом избранного `WishlistItem`, для которой он был создан.

Используется, например, для проверки избранности `Sku` на КТ.

# ReferenceAlternativeOffer

Представляет собой маппинг идентификатора `ReferenceId`, соответствующего сущности `Reference`
(в данный момент поддерживается только `SkuId` и `OfferShowPlaceId`), с альтернативными офферами `AlternativeOffer`,
для которой он был создан.

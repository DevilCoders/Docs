# Contributing

Содержание:

- [Инициализация проекта](#инициализация-проекта)
    - [Nodejs](#nodejs)
    - [Глобальные зависимости](#глобальные-зависимости)
    - [Окружение](#окружение)
        - [VSCode](#vscode)
        - [Webstorm](#webstorm)
    - [Разворачивание проекта](#разворачивание-проекта)
    - [Локальная сборка](#локальная-сборка)
- [Написание кода](#написание-кода)
    - [Коммиты](#коммиты)
    - [Создание компонентов](#создание-компонентов)
    - [Добавление иконки](#добавление-иконки)
    - [Добавление цвета](#добавление-цвета)
- [Тестирование](#тестирование)
- [Релиз версии levitan в npm](#релиз-версии-levitan-в-npm)
    - [Указать версию и опубликовать пакеты 🔥🔥🔥](#указать-версию-и-опубликовать-пакеты-🔥🔥🔥)
    - [Canary релиз](#canary-релиз)
    - [Сборка пакетов](#сборка-пакетов)
- [Обновление версии levitan в marketfront](#обновление-версии-levitan-в-marketfront)
    - [Синхронизация пакета для разработки/тестирования на локальном стенде (b2c/market)](#синхронизация-пакета-для-разработкитестирования-на-локальном-стенде-b2cmarket)

## Инициализация проекта

Как развернуть проект локально на машине разработчика.

#### Nodejs

Для того, чтобы собрать Левитан, требуется `node` версии `8.10` и выше.

#### Глобальные зависимости

В Левитане используются [yarn workspaces](https://yarnpkg.com/lang/en/docs/workspaces/) и [lerna](https://github.com/lerna/lerna).

Нужно установить `lerna` и `yarn` глобально: `npm install -g lerna yarn`

---
_**Важное примечание от 19 июля 2022 года**: Если после установки библиотек сборка проекта завершается `yarn`-ошибкой `неизвестный параметр --mutex`, попробуйте установить более старые версии `yarn` и `lerna`. У меня заработали эти:_

`npm install -g lerna@5.1.1 yarn@1.22.18`


@max-cleanen

---


### Разворачивание проекта

Для установки пакетов необходимо запустить:

```sh
npm run bootstrap
```

Либо воспользоваться пакетом ```carrier```, установив его глобально (```npm i -g @yandex-market/carrier```), после чего выполнить:
```sh
carrier
```

Если устанавливаешь/обновляешь/удаляешь зависимости (в общем, меняешь yarn.lock) с уверенностью, что они окажутся в транке, обязательно создай новый бандл с помощью кубика ```Update Bundle``` в пайплайне, иначе проверки упадут. Если у тебя Линукс, можно обновится и локально:
```sh
NO_SKY=1 carrier
```

### Окружение

Для поддрежки синтаксиса стилей нужно установить соответствующий плагин для своего редактора и включить его для `*.css` файлов.

#### VSCode

- [PoctCSS](https://marketplace.visualstudio.com/items?itemName=csstools.postcss)

#### WebStorm

<details>
<summary>Через интерфейс</summary>
<img src="https://jing.yandex-team.ru/files/antonk52/uhura_2021-06-17T13%3A37%3A21.640216.jpg" alt="включение postcss синтаксиса">
</details>

### Локальная сборка

Для запуска локальной сборки Портала:

```sh
npm start
```

## Написание кода

[Архитектура компонентного фреймворка](https://wiki.yandex-team.ru/levitan/architecture/)

[FAQ и Cookbook по написанию кода в Левитане](./COOKBOOK.md)

Одна из задач, которую стремится решить компонентный фреймворк - высокий уровень Developer Experience и качества кода. Поэтому код библиотек в Левитане должен соответствовать следующим качествам:

-   Высокое **покрытие типами**, описанные внешние интерфейсы;
-   **Документация** к компонентам, их поведению и модификаторам;
-   Своевременное **информирование** пользователей кода и подсказки об ошибках, недочетах, неточностях (см. [assertions](#assertions));
-   **Простая и понятная** реализация Компонентов
-   Доступность (a11y);
-   Эффективность и семантичность кода.

### Создание компонентов

-   `npm run yo:component`
-   Выбрать версию компонента (`core` или `product`)
    -   `core` -> ввести название нового компонента;
    -   `product` -> выбрать компонент (или несколько) и ввести название своей версии компонента (например, `myVersion`),
        затем выбрать сервис (или несколько), в котором будут созданы новые версии выбранных компонентов.

### Добавление иконки

Добавление иконки происходит в 4 шага

1. Поместите svg иконку в соответсвующий пакет по пути `packages/<NAME>/components/Icon/icons`, где `<NAME>` это `market` или `b2b`.
1. Убедитесь что имя файла в `camelCase` нотации, например `yandexPlus.svg`.
1. Выполните команду `npm run make:content:icons`.
1. Закомитьте изменившиеся файлы.

### Добавление цвета

Добавление цвета происходит в 4 шага

1. Поместите цвет в соответсвующий пакет по пути `packages/<NAME>/styles/colors/index.css`, где `<NAME>` это `market` или `b2b`.
1. Убедитесь что имя цвета не повторяется в этом файле.
1. Выполните команду `npm run make:content:colors`.
1. Закомитьте изменившиеся файлы.

### Assertions

Многие ошибки в использовании компонентов можно отловить с помощью статического анализа, типизации, но не все. Некоторые случаи статически либо сложно, либо невозможно описать, а некоторые зависят от рантайма. Для того чтобы помочь разработчикам, использующим Левитан, используются пакеты [invariant](https://www.npmjs.com/package/invariant), [warning](https://www.npmjs.com/package/warning), а также глобальная переменная `__DEV__`.

Во время сборки пакетов с помощью плагина [babel-plugin-dev-expression](https://www.npmjs.com/package/babel-plugin-dev-expression) все выражения, использующие вышеперечисленные конструкции, траспилируются в выражения, зависящие от `process.env.NODE_ENV` и `production` и соответственно при продакшн-сборке будут вырезаться, но при этом информировать разработчиков в `dev`-режиме.

## Коммиты

Для именования коммитов в Левитане используются [Conventional Commits](https://conventionalcommits.org), соответствие которым проверяется в прекоммит-хуках.

Номер тикета указывается в теле коммита. Например:

```sh
arc commit -m 'feat(core): add size modifier to the Button' -m 'MARKETFRAMEWORK-1111'
```

Допускается перечисление:

```sh
arc commit -m 'feat(market, b2b): add size modifier to the Button' -m 'MARKETFRAMEWORK-1111'
```

И уточнение до компонента:

```sh
arc commit -m 'feat(market): [Button] add size modifier' -m 'MARKETFRAMEWORK-1111'
```

### Commits & release notes

Commit message'ы используются для автоматически сгенерированных CHANGELOG'ов, поэтому стоит избегать сообщений как `review fixes` и подобные.

## Тестирование

Скриншот тестирование в Левитане осуществляется благодаря [jest-puppeteer-react](https://github.com/Hapag-Lloyd/jest-puppeteer-react), рендеринг React компонентов происходит за счет jest-puppeteer и webpack.
Для сравнения скриншотов используется [jest-image-snapshot](https://github.com/americanexpress/jest-image-snapshot).
Время и результаты выполнения можно найти в [Allure](https://a.yandex-team.ru/arc_vcs/market/front/libs/jest-allure-image-snapshot) отчете.

Плагин обрабатывает файлы с расширением `*.browser.js`, пример простейшего теста:

```
test('Button', async () => {
        await render(<Button>Button</Button>, {
            viewport: {width: 200, height: 100, deviceScaleFactor: 1},
        });
        await page.hover('button');
        const screenshot = await page.screenshot();
        expect(screenshot).toMatchImageSnapshot();
    });
```

#### Запуск тестов

Помимо [yarn workspaces](https://yarnpkg.com/lang/en/docs/workspaces/) и [lerna](https://github.com/lerna/lerna), необходимо установить [Docker Desktop](https://docs.docker.com/desktop/) (Для Linux [Docker Engine](https://docs.docker.com/engine/install/) и [Docker Compose](https://docs.docker.com/compose/install/)).

В тестах используется докер-образ из нашего приватного реестра, поэтому надо залогиниться:
1) Получить токен здесь: https://oauth.yandex-team.ru/authorize?response_type=token&client_id=12225edea41e4add87aaa4c4896431f1
2)
```
docker login -u <login> --password-stdin registry.yandex.net
<token_body><Enter>
^D(иногда может понадобиться еще один ^D, третий раз только не надо - выйдете из консоли)
```
[Инструкция по использованию Docker distribution](https://wiki.yandex-team.ru/docker-registry/)

Во время тестирования, плагин находит файлы с тестами, делает новые скриншоты и сверяет их с эталонными.
Если эталонные скриншоты отсутствуют, он записывает новые в виде эталонных.

Запуск тестов локально:
```
npm run test:screenshot
```

Запуск тестов с allure отчетом:
```
npm run report:coverage:browser
```

#### Allure report

[Allure репорт](https://github.com/zaqqaz/jest-allure) - это веб-отчет, который представляет краткую информацию о результатах тестирования.

![image](https://jing.yandex-team.ru/files/leoprog/Screen%20Shot%202021-02-12%20at%206.24.47%20PM.png)

##### Точность

Сравнение изображений выполняется с помощью pixelmatch и обладает высокой точностью. Пример обнаружения расхождений:

![image](https://jing.yandex-team.ru/files/leoprog/button-screenshot-js-button-button-test-size-l-variant-normal-1-diff.png)

#### Обновление Chrome для скриншотных тестов
Иногда нужно обновить тестовый Chrome, например, когда в Левитане начинает использоваться CSS, неподдерживаемый старыми версиями браузера. Чтобы скриншот-тесты показывали правильную вёрстку, нужно поддерживать актуальную версию браузера.
Чтобы обновить версию Chrome для локальных тестов, нужно:

1) `cd configs/jest-puppeteer-react/`
2) В [Dockerfile](/configs/jest-puppeteer-react/Dockerfile) поменять CHROME_VERSION на требуемую
3) `docker build .`
4) Скопировать sha256 нового образа

Дальше - по [инструкции](https://wiki.yandex-team.ru/docker-registry/):

5) `docker tag <sha256> registry.yandex.net/market/levitan-jest-puppeteer-react`
6) `docker push registry.yandex.net/market/levitan-jest-puppeteer-react`


## Релиз версии levitan в npm

Чтобы выпустить новый релиз, необходимо вмержить фича-ветку в trunk. 
Дальше публикация запустится автоматически в [new CI](https://a.yandex-team.ru/projects/yandexmarketfrontend/ci/releases/timeline?dir=market%2Ffront%2Fapps%2Flevitan&id=levitan-publish-release)

По умолчанию релизы публикуются как патчи (инкремент младшего числа версии, например 0.48.**0** -> 0.48.**1**).

**Если нужно поднять минор(среднее число) обязательно указать это в названии ПРа!!!**

Минор триггерится, если видит в названии ПРа подстроку ```feat(...)```. Внутри может быть один или несколько пакетов через запятую, либо несколько ```feat``` для каждого пакета. Матчится простой регуляркой, поэтому расположение не имеет значения.

После того как ветка с фичей вмержится в `trunk` и завершится пайплайн в `new CI`, можно будет найти обновленную версию пакета (например, `@yandex-levitan/market`) в [YANDEX NPM](https://npm.yandex-team.ru/-/ui/?text=@yandex-levitan/market). Вы увидите рядом с названием пакета приписку вроде `0.48.0 published at Tue Jul 19 2022`. Это значит, что публикация прошла успешно и новую версию `@yandex-levitan/market` можно "апнуть" в вашем проектном `package.json`.



Ручная публикация может потребоваться, если ты ответственный за поддержку Левитана.

Новые версии выпускаются если:

- Ваш pull request был вмержен
- Вы находитесь на ветке `trunk`
- Вы залогинены в яндексовый npm
- Вы владеете правами на публикацию пакетов

Чтобы посмотреть, кто может выпустить новую версию, нужно выполнить команду

```sh
npm owner ls @yandex-levitan/core
```

**Указывать версию в package.json на ветке разработчика НЕ НУЖНО**

Для публикации levitan нужно указать версию и опубликовать собранные пакеты. Ниже описывается из чего состоят эти шаги с примерами.

### Указать версию и опубликовать пакеты 🔥🔥🔥

Номер версии определяется в соответствии с [Semantic Versioning](https://semver.org/).

<details>
<summary>Для публикации нужно быть авторизованным во внутреннем npm.</summary>

Для этого нужно выполнить команду (предварительно сгенерировав OAuth-токен [здесь](https://oauth.yandex-team.ru/authorize?response_type=token&client_id=f8aaf2afb26446ee99a7fab5e92057a5)):
```
npm login --registry=https://npm.yandex-team.ru
```

</details>
<br />

**Убедитесь что у вас есть права на пуш в master ветку**

Для выпуска новой версии, находясь в чистом свежем транке, воспользуйтесь одной из команд

- `npm run publish` - выпустить автоопределенные версии пакетов
- `npm run publish:major` - выпустить мажорную версию и патч для зависимостей
- `npm run publish:minor` - выпустить минорную версию и патч для зависимостей
- `npm run publish:patch` - выпустить патч версию для всех пакетов

Каждая из этих команд выполняет следующее:

1. Проверит, что все условия для публикации выполнены
2. Создаст ветку по тикету и зачекаутится
3. Обновит версии затронутых пакетов
4. Обновит левитановские зависимости обновленных пакетов
5. Сбилдит публикуемые пакеты
6. Опубликует пакеты
7. Закоммитит изменения
8. Создаст ПР с упрощенным флоу, ссылку выведет в консоли

Нужно просто дождаться мержа ПРа. Не забыть, что находишься в публикуемой ветке.

### Canary релиз

Полезен если хочется выпустить канари версию фичи с ветки разработчика, до того как она была влита в мастре, что бы собрать демостенд(тестинг/престейбл) и отдать тестировщику.

Если ты простой разраб и у тебя нет прав на публикацию, то смело создавай ПР, в ci находи кубик Canary Publish и стартуй его.

Если с правами все ок, то, находясь на фича ветке, выполни:

```sh
npm run publish:canary
```

Можно выпускать несколько версий пакета для указанного тикета.

### Сборка пакетов

Обычно руками выполнять сборку пакетов **не требуется**, так как это происходит автоматически при изменении версии. Выполняется следующая команда:

```sh
lerna run build
```

Если необходимо собрать определенные пакеты, следует использовать флаг `--scope`, например:

```sh
lerna run build --scope @yandex-levitan/core
```

Для выполнения команд в пакетах можно также использовать `lerna exec`, например:

```sh
lerna exec --parallel -- yarn
```

## Обновление версии levitan в marketfront

Когда публикуется новая версия levitan (не важно, релизная или canary), чтобы получить нужные изменения в marketfront, необходимо обновить пакеты @yandex-levitan в файлах package.json и package-lock.json репозитория marketfront.

**Для локального стенда на логрусе достаточно синхронизации содержимого npm-пакетов @yandex-levitan напрямую в node_modules стенда!** (см. [пункт ниже](#синхронизация-пакета-для-разработкитестирования-на-локальном-стенде-b2cmarket))

Однако для создания корректного PR и поднятия мультитестинг-стенда marketfront необходимо выполнить следующие действия:
1. Опубликовать новую версию levitan ([canary](#canary-релиз) или [релизная](#релиз-версии-levitan-в-npm))
2. Указать в корневом package.json в marketfront нужную версии пакетов @yandex-levitan/market из шага 1.
    <details>
    <summary>Как узнать нужную версию</summary>

    - **Последняя релизная версия** - выполнить команду `npm view @yandex-levitan/market`
    - **Релизная версия по конкретному тикету** - из [CI Levitan](https://a.yandex-team.ru/projects/yandexmarketfrontend/ci/releases/timeline?dir=market%2Ffront%2Fapps%2Flevitan&id=levitan-publish-release)
    - **Сanary** - из логов кубика Publish Canary внутри проверок PR

    </details>
3. Временно удалить из корневого package.json репозитория marketfront скрипты preinstall и postinstall
4. Запустить `npm install --package-lock-only`
5. **После окончания установки проверить package-lock.json на наличие задублированных строк <span style="color: red">npm:</span> в названиях пакетов. Дублирования удалить!**
Чаще всего это происходит с пакетом gemini-core
    <details>
    <summary>Пример дублирования</summary>
    <img src="https://jing.yandex-team.ru/files/shkaburavv/photo_2022-06-02%2017.33.23.4d7df41.jpeg" alt="Дублирование npm: в gemini-core">
    </details>
6. Вернуть скрипты preinstall и postinstall в корневой package.json (удалены на шаге 2)
7. Опубликовать PR с изменениями в package.json и package-lock.json
8. Дождаться поднятия мультитестинга, протестировать на нем изменения

### Синхронизация пакета для разработки/тестирования на локальном стенде (b2c/market)

Для синхронизации библиотеки на локальном стенде (eg. logrus) можно выполнить команду, не выпуская библиотеку.
Для этого потребуется:

1. один раз создать локально файл `paths.bash` в корне проекта.
Файл создает переменные окружения с путями до локально собранного пакета и папкой назначения.

```bash
#!/usr/bin/env bash

USERNAME=`whoami`

# Путь до пакетов Левитана на локальной машине.
# Исправьте, если у вас путь отличается!
export SYNC_SRC="/Users/$USERNAME/arcadia/market/front/apps/levitan/packages/"

# Путь до npm-пакета Левитана в marketfront на логрусе.
# Исправьте, если у вас путь отличается!
export SYNC_DEST="$USERNAME@market.logrus01hd.yandex.ru:marketfront_$USERNAME/node_modules/@yandex-levitan/"
```
_P.S. Пример для b2c части_

2. Выполнить команду
```sh
npm run build:sync
```

Команда соберет все пакеты и отправит на сервер необходимый пакет.
Синхронизация использует библиотеку rsync для единоразовой синхронизации после сборки.

#### Синхронизация пакетов для разработки/тестирования на локальной машинке (b2b)

Для синхронизации библиотеки локально можно выполнить команду, не выпуская библиотеку.
Для этого потребуется:

1. один раз создать локально файл `marketpartner-paths.bash` в корне проекта.
Файл создает переменные окружения с путями до локально собранного пакета и папкой назначения.

Пример:

```bash
#!/usr/bin/env bash

export SYNC_PARTNERNODE_DIRNAME="/Users/<username>/Workspace/partnernode" (абсолютный путь до партнерки)
```

2. Выполнить команду
```sh
npm run build:sync-marketpartner
```

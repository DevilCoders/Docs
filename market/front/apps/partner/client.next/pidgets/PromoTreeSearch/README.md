# PromoTreeSearch 

🏗️ В процессе разработки

## Что это?

TreeSearch, позволяющий инкапсулировать подгрузку начальной информации

## Поддержка

Поддерживается контуром [B2B Нерекламное продвижение](https://abc.yandex-team.ru/services/promotionberu/).

## Особенности использования

### Создание пиджета 
Для использования пиджета нужно создать его инстанс с помощью фабрики `createPromoTreeSelector`.
Фабрика принимает два параметра:
- `name` - уникальное имя пиджета
- `resolveItems` - резолвер, который приносит дерево, с которым будет работать селектор, он прокидывается в эпики и приносит начальное состояние пиджета(если оно не было передано явно).
Для корректной работы пиджета, id корня дерева сущностей, которое возвращает резолвер, должно соответствовать константе "rootId"

### Получение массива id выбранных сущностей
В случае, если от пиджета нужно получить массив id выбранных сущностей, можно взять `excluded`, `selection` и `itemsTree`, которые отдаёт onChange пиджета, с помощью них и утилиты `selectionToFlatList` из `shared/utils/fulfillmentLoyalty/index.ts` получить итоговый массив идентификаторов.

Такой подход был выбран, чтобы избавится тяжелой операции пересчёта массива id на каждое срабатывание onChange. Также, может понадобится получить из пиджета именно массивы excluded и selected, которые нельзя однозначно восстановить из массива id и дерева категорий, а с таким подходом это сделать возможно.

Для ситуации, когда нужно получить дерево, которое использует селектор, при загрузке пиджета, предусмотрен коллбэк onTreeLoad, срабатывающий при подгрузке этого дерева. Сейчас коллбэк срабатывает только при инициализации пиджета.


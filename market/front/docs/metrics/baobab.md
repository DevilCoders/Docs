# Baobab

__Baobab__ - это технология логирования логической структуры страницы и пользовательских действий. В рамках этой технологии логи перекладываются между несколькими внутренними сервисами (ClickDaemon, LogBroker, LogFeller) и попадают в YT-таблицы

[Папка с baobab-логами Яндекс Маркета в YT](https://yt.yandex-team.ru/hahn/navigation?path=//logs/market-baobab)

Для фронтенд-разработчика Маркета обычно не важна промежуточная структура. Важно как переданные с браузера логи превращаются в записи в YT. Для этого аналитики и фронтендеры договариваются о структуре передаваемых логов и фиксируют их в специальном документе - [RFC](https://wiki.yandex-team.ru/baobab/rfc/market)

## Виды баобаб событий

Баобаб отправляет с приложения объекты, которые описывают некоторые события, произошедшие в системе. Объект события содержит поле `event` в котором находится тип события. События, отправляемые Баобабом, можно разделить на два вида: структурные события и действия.

#### Логическая структура страницы

В отличие от других систем логирования, Баобаб строит дерево логических узлов на странице. Выглядеть оно может примерно так
```json
{
    "id": "1",
    "name": "$page",
    "attrs": { ...какие-то данные },
    "children": [
        {
            "id": "2",
            "name": "snippet",
            "attrs": {},
            "children": [ ... ]
        },
        ...
    ]
}
```
Каждый узел имеет id, который генерируется по requestId и название. Он также может иметь дочерние узлы (дерево же) и какой-то набор данных. Все это будет залогированно баобабом во время загрузки страницы.

Страницы в приложения могут содержать динамические элементы, поэтому баобаб отдельными структурными событиями дописывает новые поддеревья в существующий узел по его id.

Бывают такие структурные события (объекты описан не полностью):
- `{event: "show", tree: { ... root-узел дерева }}` - событие показа дерева. Оно логирует первичное состояние дерева.
На каждую загрузку страницы такое событие может быть только одно.
- `{event: "append", tree: {attrs: {"parent-id": "куда добавляем"}, children: {что добавляем}}` -
событие добавление поддерева в конкретный существующий узел
- `{event: "visibility", type: "hide", block-id: "id"}` - событие того, что узел id="id" был скрыт вместе со всем поддеревом
- `{event: "visibility", type: "show", block-id: "id"}` - противоположное предыдущему событие показа

В баобабе нет события удаления узла, только скрытие.

#### Действия на странице

Баобаб поддерживает 3 событий логирования действий:
- `{event: "click", id: "node id"}` - клик по узлу `"node id"` заранее залогированного дерева
- `{event: "scroll", id: "node id"}` - проскрол узла `"node id"` заранее залогированного дерева
- `{event: "tech", type: "your type", id: "node id"}` - техническое событие какого-то узла (например root-узла) с произвольным типом

Каждое событие действия может содержать поле `data`, в которое можно положить какие-то данные этого события.

[Дополнительно можно почитать про события в общей части RFC](https://wiki.yandex-team.ru/baobab/rfc/common)

## Как пользоваться

Баобабные узлы создаются при помощи зон. Для того, чтобы зона считалась баобаб-узлом, нужно в настройках зоны указать `mode: 'baobab'`
```tsx
<Zone name="snippet" mode="baobab">...</Zone>

// or

withZone({name: 'snippet', mode: 'baobab'})(...)
```
`mode: 'baobab'` указывает зоне создать баобаб узел с указанным именем и данными. Но такой `mode` __не логирует зону в метрику__.

Для того, чтобы зона логировалась в метрику и создавала баобаб-узел, нужно указать `mode: 'both'`
```tsx
<Zone name="snippet" mode="both">...</Zone>

// or

withZone({name: 'snippet', mode: 'both'})(...)
```
Это полезно, когда для компонента уже определена зона и нужно начать логировать баобаб-узел по этой зоне.

Довольно часто нужно логировать баобаб-узлы с одним именем, но для компонентов уже определена зона с другим именем. Чтобы не ломать уже существующие метрики, но поменять имя баобаб-узла, можно использовать параметр `baobabNodeName`
```tsx
<Zone name="snippet" baobabNodeName="$result" mode="both">...</Zone>

// or

withZone({name: 'snippet', baobabNodeName: '$result', mode: 'both'})(...)
```

Все redux-экшены, которые обрабатываются `@yandex-market/cia` логируются в баобаб как `tech` событие. Название redux-экшена используется как `type` события, а payload передается в `data`. Также cia находит ближайший React-компонент, обернутый в Zone поддерживающую Баобаб и использует id этого узла в событии.

Только для баобаба можно залогировать координаты расположения узла на странице. Для этого нужно в настройки зоны передать `calcCoords: true`. Это настройка положит в `attrs` баобаб-узла поле `coodrs` в формате `[x, y, width, height]`. __Работает только с `mode: 'baobab'` и `mode: 'both'`__.

Все остальные правила, такие как `withClickSensor`, `withVisibilitySensor` и тд, применяются, как описано в гайде про зоны.

## Debug

Для того чтобы дебажить события, которые отправляются Баобабом, нужно добавить `exp_flags=validate_counters`. Это общее название флага включения дев-тулов Баобаба во всех сервисах Яндекса с Баобабом.

В левой части экрана появятся 2 кнопки: кнопка с `B` и с `🌱` внутри.

Кнопка `🌱` открывает в соседней вкладке страницу с отображением текущего баобабного дерева. Это общий devtool, который предоставляется из коробки с баобабом. Эта страница синхронизирована со страницей, откуда ее открыли. Это позволяет автоматически обновлять дерево по структурным событиям. В дереве при наведении на `i` рядом с названием узла можно увидеть данные, которые привязаны к этому узлу.

Кнопка `B` открывает Маркетный девтул баобаба.

![Внешний вид девтула](./_assets/devtool_view.jpg)

Дерево в девтуле динамически изменяется согласно баобаб событиям, которые происходят на страницы. Если узел был скрыть (произошло событие visibility hide с этим узлом), то в devtool это узел (и все его поддерево) будет серым и перечеркнутым

![Описание узла девтула](./_assets/devtool_main.png)

Если узел содержит дочерние узлы, то слева от названия будет отображаться стрелка __(1)__. При нажатии на стрелку дочерние узлы разворачиваются, либо сворачиваются.

При наведении на название узла __(2)__ у блока в верстке, который соответствует этому узлу в верхнем левом углу появляется табличка с названием узла и его id. При клике на название узла в devtool браузер подскролит страницу до места расположения блока в верстке этого узла, если узел не скрыт

Рядом с названием узла могут быть 2 дополнительные иконки: Е __(3)__ и А __(4)__, при наведении на которые появляется еще одно окно с дополнительной информацией.
- При наведении на E отображается таблица всех событий которые связаны с этим узлом. Это могут быть события, которые произошли с самим узлом (например, клик по нему), либо события, которые произошли на родительском узле и связаны с текущим (например, скрытие или показ). В колонке `initiator` указано, какой узел инициировал событие
- При наведении на A отображаются атрибуты узла в формате JSON. Этой иконки может не быть, если у узла нет атрибутов

По умолчанию, если убрать мышку с иконки, панель скрывается. Если кликнуть на иконку, то панель закрепится и не будет скрываться, а буква в иконке изменится на X. Чтобы открепить панель, нужно кликнуть на иконку еще раз. Если кликнуть на другую не закрепленную иконку, то закрепленная панель заменится.

В верхней части devtool есть 3 дополнительные кнопки

![Описание тулов девтула](./_assets/devtool_toolbar.png)

- __(1)__ - раскрывается все узлы всего дерева
- __(2)__ - скрывает все узлы всего дерева
- __(3)__ - прикрепляет панель к левой части экрана. При клике на кнопку второй раз панель возвращается в правую часть.

# Информация о CI проверках в marketfront

## Обязательные проверки

Список обязательных проверок можно увидеть включив фильтр `required` в плашке:

![img.png](_assets/ci-required.png)

Обязательные проверки связанные с фронтом, дополнительно помечены эмодзи - 🔒
Для перехода в пайплайн достаточно перейти по ссылке в плашке.

#### Жди релиза [Может снять дежурный]

По-умолчанию проверка будет красной - и это нормально, мы не мержим ревью в транк напрямую.
Код попадает в транк через релизный процесс: https://docs.yandex-team.ru/marketfront/workflow/releases
В случае необходимости (изменение скриптов, документации) - люди из группы https://abc.yandex-team.ru/services/yandexmarketfrontend?scope=devops могут озеленить проверку.

#### Общие проверки

![img.png](_assets/ci-sample.png)

На примере `flow` проверки:
- последний кружок ссылается на текстовый/html отчет проверки
- средний кружок ссылается на `stdout/sterr` проверки
- первый кружок ссылается на `Sandbox` задачу, в рамках которой была проведена проверка

{% note info %}

Перезапуск проверки осуществляется при помощи последней иконки в кубике

{% endnote %}

#### Упавшая проверка

![img.png](_assets/ci-failed.png)

Если создался отчет - заходим в него и видим причину падения.

![img.png](_assets/failed-jest.png)

Если отчет не создался - читаем логи `Sandbox` задачи.


## Необязательные проверки

![](_assets/not-required.png)

Если с проверки можно снять галочку (или ее там нет по-умолчанию), то проверка не считается обязательной.
Все новые проверки добавляются сначала в необязательные и только после того, как их стабильность подтверждена могут быть перенесены в обязательные.

## Prepare script
### How to use it?
If service is configured with Instance Containers add a new init-container runs the following shell-script
```
tar -zxf market-common-config.tar.gz && bash bin/prepare.sh {BSCONFIG_INAME}
```

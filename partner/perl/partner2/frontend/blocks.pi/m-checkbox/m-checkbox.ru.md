### Описание

Моделька, отражающая состояние чекбокса.

Список полей:

* `checked` `boolean` Отражает состояние чекбокса (отмечен или нет).
* `value` `string` Хранит значение атрибута `value` чекбокса.

### Зависимости

* `bem-mvc`.

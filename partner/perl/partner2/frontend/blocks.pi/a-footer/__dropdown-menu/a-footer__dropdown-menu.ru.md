Элемент-выпадающее меню. Использует блок `dropdown-menu`. Можно также использовать поля: **menu**, **menuTitle**, **label** (последний описывает содержимое элемента `switcher` блока `dropdown-menu`). Данные поля работают, если содержимое не задано явно через поле `content`.

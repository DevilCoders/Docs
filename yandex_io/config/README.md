Здесь лежат базовые конфиги. Они должны использоваться для построения конечных файлов `quasar.cfg`. Файл `common.cfg` -- общий конфиг для всех (в т.ч. проектов из `smart_devices`) `yandex_io_common.cfg` -- надстройка первого уровня для проектов из `yandex_io`.


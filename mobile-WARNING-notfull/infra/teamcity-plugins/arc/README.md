Install local teamcity:

`./gradlew installTeamcity2020.2`

Start local teamcity:

`./gradlew startTeamcity2020.2Server`

Teamcity UI available at http://localhost:8111

Deploy plugin to local teamcity:

`./gradlew deployToTeamcity2020.2`

Stop local teamcity:

`./gradlew stopTeamcity2020.2Server`

#Change log.

## 0.15
MOBDEVTOOLS-601: починили параллельный фетч по префиксам.

## 0.14
Распараллелили collectingChanges: считаем чейнджи по отдельный веткам в несколько потоков.
MOBDEVTOOLS-231 Сделан паралельный фетч веток по нескольким префиксам. Сделан префетч веток в отдельном потоке.

## 0.13
MOBDEVTOOLS-516: Добавлены параметры `yandex.arc.processed.branch.limit.enabled` и `yandex.arc.process.branch.limit.value` для управления лимитами на обработку веток.
MOBDEVTOOLS-515: Выключение сбора изменений теперь проверяется для каждой обрабатываемой ветки, а не для пачки целиком.
MOBDEVTOOLS-514: Добавили счетчики `BranchForProcessingCount` и `ProcessedBranchCount` для мониторингов количества веток планируемых к обработке в очередном батче и фактически обработанных.
MOBDEVTOOLS-518: Всегда используем GRPC API для работы плагина. Старый код по работе с Arc CLI полностью удален.
MOBDEVTOOLS-125: Удалили неиспользуемый код по сборке патчей из плагина

## 0.12
MOBDEVTOOLS-101:
Добавили метрики на ручки getCurrentState и collectingChanges.
Переписали основные ручки на использование GRPC: текущее состояние веток репозитория, сбор списка изменений между двумя состояния репозитория, контент файла и листинг директории.
Добавили опцию `yandex.arc.use.grpc.arc.api` для включения GRPC.
Добавили опции `yandex.arc.trunk.log.limit`, `yandex.arc.special.branch.log.limit`, `yandex.arc.normal.branch.log.limit` для раздельного управления размером запрашиваемой истории для транка, бранчей мобильной машинерии и обычных бранчей соответственно. Работает только для GRPC реализации.
Добавили опцию `yandex.arc.file.content.limit` ограничивающую максимальный размер запрашиваемого через Arc Public API файла.

## 0.11
MOBDEVTOOLS-173:
Удалили опции `arc.fetch.quite.period.enabled` и `arc.fetch.quite.period.ms`
Добавили опцию `yandex.arc.use.current.state.batch.cache` для режима отдачи состояния из первого завершенного запроса остальными запросами из пачки.

## 0.10
MOBDEVTOOLS-116: Добавили возможность неблокирующего вызова для некоторых типов запросов.
Так же не фетчим все изменения на каждый вызов getCurrentState.
MOBDEVTOOLS-121: Берем только первого родителя из списка родителей коммита.

## 0.9
Перенесли кеши из pluginData в caches. https://st.yandex-team.ru/QAFW-3674
Лимитируем создание VCS рутов. https://st.yandex-team.ru/DEVTOOLS-7157
Изменен сбор чейнджей для новых и /rebase веток: теперь всегда запрашиваются последние 20 коммитов, если это не переопределено в настройках. https://st.yandex-team.ru/DEVTOOLS-6245

## 0.8 Технический релиз
Выпиливаем номер версии из имени архива.
Апаем версию, так как есть проблемы с обновлением на teamcity.

## 0.7.1 Чиним упячку
Пофикшен постоянный расчет чейнджей в /rebase ветках даже если ветка не изменилась. https://st.yandex-team.ru/DEVTOOLS-6861

## 0.7 Ускорение
Ускоряем сборку изменение плагином через использование `arc log --name-status` вместо раздельного лога коммитов и сборки изменения по каждому коммиту через arc show.
Лимитируем максимальный размер списка коммитов обрабатываемого для ветки в 100 коммитов.
Тикет: https://st.yandex-team.ru/DEVTOOLS-6801

## 0.6 Доработки админки
* Добавил список VCS Roots на базе Arc вместе со списком проектов, в которых оные используются.
* Админка теперь доступна для просмотра для всех людей с ролью Project Administrator.
* Улучшили выключение операций с Arc через internal.properties.

## 0.5 Поддержка rebase бранчей
* Собираем изменения для бранча вида /users/robot-stark/{prId}/rebase от бранча /users/robot-stark/{prId}/to.

## 0.1 Базовая функциональность.
* Базовое API для работы с Arc + Teamcity.
* CI.
* Мониторинги.
* Заготовка админки.
* Поддержка Internal properties в Teamcity для выключения плагина.

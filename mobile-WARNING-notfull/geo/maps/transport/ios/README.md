# Яндекс.Транспорт 🚌

Репозиторий iOS клиента приложения Яндекс.Транспорт.

## Начало работы

Инструкция по сборке приложения лежит [здесь](https://wiki.yandex-team.ru/jandekskarty/programma-peshexodov/mobile-transport/Mobilnaja-razrabotka/Getting-started/).

## VCS

- master — основная ветка разработки
- release-\<version> — ветка конкретного релиза, из последнего коммита которой будет собран релиз с соответствующей версией
- feature/ — префикс веток для задач релиза
- bugfix/ — префикс веток с исправлением багов
- Разработка ведется через PullRequest'ы. Формат описания лежит [здесь](https://wiki.yandex-team.ru/jandekskarty/programma-peshexodov/mobile-transport/Mobilnaja-razrabotka/Format-opisanija-pull-request-ov/).
- При возникновении конфликтов rebase предпочтительнее merge'a.
- Коммиты на английском языке. Каждый коммит предваряется префиксом \<issue-number>:, где issue-number — номер задачи в  ST. В название ветки номер задачи не включается, так как он есть в коммитах.

## Метрики

Алгоритм работы с метриками следующий: [здесь](https://wiki.yandex-team.ru/jandekskarty/programma-peshexodov/mobile-transport/metrics/) лежит сводная таблица с метриками для обеих платформ. Когда аналитик добавляет/редактирует событие в ней, она скачивается и кладется в корень проекта. Затем вызывается скрипт metrics.sh, который тоже лежит в корне. Он генерирует по таблице файлы YXAppAnalytics+Gena.h и YXAppAnalytics+Gena.m. В файлах содержится протокол со всеми событиями из таблицы и имплементация его классом YXAppAnalytics аппметрики.

## Эксперименты

Платформой для проведения A/B экспериментов служит MapKit. Текущие эксперименты на проде транспорта заведены [здесь](https://ab.yandex-team.ru/config/70/version/96). Для доставки заведенных экспериментов на клиент используется мапкитовая сущность YMKUiExperimentsManager и обертки над ней. Для тестовых сборок есть возможность указать значение эксперимента вручную, с помощью Дебаг-панели — экрана в настройках.

## Переводы

Переводы строк лежат в [танкере](https://tanker.yandex-team.ru/?project=mobile_transport_ios&branch=master&keyset=..%2Fres%2Fios_translations%2FLocalizable). Для синхронизации переводов в танкере и приложении есть скрипт, который лежит в lib/modules/TankerTool и написан на swift. Для начала скрипт нужно сбилдить, выполнив команду "swift build" в папке TankerTool. Для синхронизации нужно выполнить команду "swift run tanker-tool ../../../TankerToolConfig.json" — скрипт найдет все NSLocalizedString в проекте, выгрузит из них ключи в танкер, и выгрузит из танкера готовые переводы в файлы Localizable.strings. При первом запуске синхронизации нужно добавить флаг инициализации и ключ: "swift run tanker-tool ../../../TankerToolConfig.json \-\-init \<ключ>". 

## Зависимости

Все новые зависимости подключаются с помощью cocoapods.

Часть зависимостей подключается с помощью скрипта reload_frameworks.sh, лежащего в корне проекта. Он скачивает завсимости и добавляет их фреймворками в проект.

Часть совсем старых зависимостей, перекочевавших из карт, живет в папке lib.

## Разработка

Актуальный кодстайл лежит [здесь](https://bb.yandex-team.ru/projects/TRANSPORT/repos/transport-client-ios/browse/documentation/styleguide/StyleGuide.md). 

### Архитектурный дизайн

Проекту больше 5 лет и его писали разные команды с разным подходом, поэтому какого-то осмысленного и консистентного архитектурного дизайна у проекта нет. Однако есть большие пласты похожим образом написанного кода, который будет описан в этой секции.

### MapKit

Вся core функциональность приложения написана вокруг MapKit'a. Вью карты лежит в MapViewController, который добавлен чайлдом в RootViewController и не выгружается из памяти на протяжении времени жизни приложения. 

Взаимодействие с картой осуществляется через класс MapKitMapFacade. Он отвечает за работу с камерой, зумами, выделением/снятием выделения объектов подложки и оповещением о тапах. 

Для работы с коллекциями объектов на карте существуют экстеншены (MapLayerStopsExtension, HomeAndWorkExtension и т.п.). Они отвечают за отображение объектов на карте, действия при тапе на эти объекты и, зачастую, за подгрузку объектов, не входящих в подложку. В зависимости от сценария, какие-то экстеншены включаются, а какие-то отключаются. Это регулируется в MapViewController'e в методе  func setExtensions(state: MapExtensionsState). 

Для построения маршрутов используются сущности MapKit'a: YMKDrivingRouter, YMKMasstransitRouter и YMKPedestrianRouter. Работа с ними ведется через соответствующие обертки в lib/YandexGeo.

Для поиска используется сущность MapKit'a YMKSearchManager. 

### Зависимости

Бо́льшая часть зависимостей создается в сущности Bootstrapper на старте приложения, там же происходит их инжектирование. Часть зависимостей свалены в DefaultAppContext, который протаскивается из экрана в экран.

### Навигация

RootViewController — корневой контроллер окна приложения. Содержит чайлдами MapViewController и MapAndSearchViewController. 

MapAndSearchViewController отвечает за главный экран. В него чайлдом добавляется SearchViewController, у которого есть несколько состояний, и RouteSuggestViewController — контроллер маршрутных саджестов(куда, домой и тд). Поверх MapAndSearchViewController может презентиться MenuViewController, RoutingConfigurationViewController и контроллер одной из карточек объекта на карте.

MenuViewController — центральная точка входа для побочных сценариев приложения, вроде настроек, кабинета и промокодов. Презентится в своем навигационном стеке. Для открытия какого-то конкретного раздела (например, для перехода по DeepLink'у) используется сущность MenuRouter.

RoutingConfigurationViewController — начало сценария построения маршрута. С него можно осуществить поиск точек откуда/куда, задать параметры маршрута(время, виды транспорта) и перейти на горизонтальную выдачу маршрутов. Презентится в своем навигационном стеке.

Карточки объектов на карте в большинстве своем являются наследниками класса MapLayerObjectDetailsViewController и добавляются в CardStackController, который управляет максимальной вложенностью карточек.


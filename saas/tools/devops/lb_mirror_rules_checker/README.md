Checks that our services (their topics) have correctly configured remote mirror rules (if mirroring enabled).

What has been done so far:
1. Checks that mirror rules were created by saas-robot token.

TODO:
1. Implement checks that topic mirror rules set is unique for the given source topic.
- https://st.yandex-team.ru/LOGBROKER-7010 # <- or push this ticket instead

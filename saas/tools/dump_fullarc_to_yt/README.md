Инструмент для получения таблички из ключей, kps, значений, таймстемпов.

Теперь можно запускать в YT и локально.

* Пример запуска в YT: `./dump_fullarc_to_yt  --mode yt --index-dir //tmp/derrior/videotop-data --output //tmp/derrior/videotop-result1 --yt arnold`

```
SELECT
    date,
    timestamp,
    project,
    service,
    page,
    platform,
    url,
    url_id,
    vhost,
    environment,
    test_ids,
    exp_flgs,
    browser_engine,
    browser_engine_version,
    browser_name,
    browser_version,
    browser_version_major,
    browser_base,
    os_family,
    os_name,
    os_version,
    os_version_major,
    device_name,
    device_vendor,
    in_app_browser,
    is_robot,
    is_tv,
    is_tablet,
    is_touch,
    is_mobile,
    is_adblock,
    version,
    region,
    request_id,
    request_id_hash,
    1234567 as yandexuid,
    kv_keys,
    kv_values,
    is_internal,
    cdn,
    'ipv6' as ipv6,
    message,
    message_id,
    original_message,
    error_urls_keys,
    error_urls_values,
    runtime,
    level,
    file,
    file_id,
    block,
    method,
    line,
    col,
    'stack_trace' as stack_trace,
    stack_trace_id,
    'original_stack_trace' as original_stack_trace,
    stack_trace_names,
    stack_trace_urls,
    stack_trace_lines,
    stack_trace_cols,
    user_agent,
    user_agent_id,
    source,
    source_method,
    source_type,
    client_timestamp,
    replaced_url_keys,
    replaced_url_values,
    parser,
    123456 as icookie,
    silent,
    dc,
    coordinates_latitude,
    coordinates_longitude,
    coordinates_precision,
    coordinates_timestamp,
    host,
    client_init_timestamp,
    logged_in,
    logbroker_topic
FROM errors
WHERE
    (project = 'video-player' OR project == 'video-player-iframe-api')
  AND date between toDate(1657632380) AND toDate(1657633880)
  AND timestamp BETWEEN 1657632380 AND 1657633880
  AND kv_values[indexOf(kv_keys, 'vsid')] = 'vsid';
```
# Турбо-страницы 🚀

[Чеклист разработчика турбо](docs/DeveloperChecklist.md)

## Содержание
* [Разработка](#разработка)
   * [Запуск проекта](#запуск-проекта)
   * [Разные среды запуска](#разные-среды-запуска)
   * [В рамках СЕРПа](#в-рамках-серпа)
   * [CGI-параметры](#cgi-параметры)
   * [Параметры только в Templar](#параметры-только-в-templar)
   * [Features (предобработка корневых данных)](#features-предобработка-корневых-данных)
   * [Переводы](#переводы)
* [Версионирование разметки и стилей](#версионирование-разметки-и-стилей)
* [Дизай-ревью](#дизай-ревью)
* [Документация](#документация)
   * [Hermione-тесты](#hermione-тесты)
      * [Включение рекламы и аналитики](#включение-рекламы-и-аналитики)
      * [Отключение дозагрузки страниц](#отключение-дозагрузки-страниц)
      * [Отключение айфреймов](#отключение-айфреймов)
      * [Стаб времени запроса](#стаб-времени-запроса)
      * [Ручки к темплару для тестов гермионы](#ручки-к-темплару-для-тестов-гермионы)
      * [Патчи для изменения данных](#патчи-для-изменения-данных)
      * [Переименование тестов](#переименование-тестов)
      * [Стабилизация тестов (погонятор, castle)](#стабилизация-тестов-погонятор-castle)
      * [Лучшие практики](#лучшие-практики)
      * [Автоматизация работы с тестами](#автоматизация-работы-с-тестами)
* [Данные](#данные)
   * [Загрузка данных в SaaS](#загрузка-данных-в-saas)
      * [Удаление данных из SaaS](#удаление-данных-из-saas)
   * [Загрузка данных в SaaS способ 2](#загрузка-данных-в-saas-способ-2)
   * [Стабы](#стабы)
      * [Для BEMHTML-блоков:](#для-bemhtml-блоков)
      * [Для React-блоков:](#для-react-блоков)
   * [Метрика](#метрика)
* [Соглашения](#соглашения)
* [Ревью](#ревью)
* [Полезные ссылки](#полезные-ссылки)

## Разработка

[Документация про React в Турбо](https://wiki.yandex-team.ru/turbo/front/react/)
### Запуск проекта
Должен быть установлен [Templar](https://github.yandex-team.ru/search-interfaces/infratest/tree/master/packages/templar).

```bash
[Смонтировать аркадию](https://wiki.yandex-team.ru/search-interfaces/arc/)
cd arcadia/frontend/services/turbo
nvm use # мы рекомендуем использовать версию node.js из .nvmrc
npm i -g npm@6.14.5 # актуальная версия NPM указана в package.json в "engines.npm"
npm ci
make ## собрать все платформы
npm start
open http://localhost:3333/
```

Для запуска публичной беты
```
npm run public
```

Собрать одну платформу:
```
PLATFORM=touch-phone make
PLATFORM=desktop make
```

Пример турбо-страницы: http://localhost:3333/turbo?text=test_news — платформа определяется автоматически на основе юзерагента на бекенде и приходит на вёрстку в поле `platform`.

### Установка нового пакета

У нас в проекте используется Lerna. При использовании `npm install package` есть проблемы с очисткой пакетов, установленных через `lerna bootstrap`, поэтому сборка проекта может упасть. Для установки нового пакета необходимо использовать команду:
```
lerna add package-name
```

### Разные среды запуска
Иногда есть необходимость запускать проект не только в среде разработки, но также в тестинге и продакшине. В этом случае необходимо собрать проект в тестинге (`production` и `testing` окружения локально не различиются):
```sh
YENV=testing make build
```
или запустить команды последовательно, так, как [это делает sandbox](https://a.yandex-team.ru/arc/trunk/arcadia/sandbox/projects/sandbox_ci/sandbox_ci_turbo/__init__.py?rev=4940683#L76-78):
```sh
YENV=testing make node_modules;
YENV=testing make config;
YENV=testing make build-pages;
```
Затем необходимо выполнить запуск проекта:
```
NODE_ENV=testing templar start (--public)
```
Тесты можно запустить так же:
```
NODE_ENV=testing hermione gui (...)
```

### В рамках СЕРПа
Основной репозиторий СЕРПа, куда нужно делать пулреквесты: https://github.yandex-team.ru/serp/web4

Общий форк для разработки: https://github.yandex-team.ru/search-interfaces/web4

[Описание специальных возможностей для разработки турбо в СЕРПе](https://github.yandex-team.ru/serp/web4/blob/dev/src/vendors/turbo/README.md).

### CGI-параметры
Это CGI параметры для турбо. Если нужно пробросить CGI параметры в турбо с серпа - вам [сюда](https://github.yandex-team.ru/serp/web4/blob/dev/src/vendors/turbo/README.md#проброс-дополнительных-cgi-параметров-в-турбо)

* `&stub=page/kinopoisk.json` — подробнее в [отдельной секции](#стабы)
* `&export=json` — выводит все данные от бекенда
* `&gimme=json` - аналог `&export=json`, реализованный на вёрстке
* `&dump=eventlog` — выводит в конце страницы лог работы источников данных
* `&dump=eventlog&dbg=1` — помимо лога источников, выводит в конце станицы ещё и их ответы
* `&exp_flags=analytics-disabled=0&_ym_debug=1` — выводит в консоль логи метрики
* `&test-mode=1&no-tests=1` — отключение всех экспериментов
* `&turbo_enable_cgi_forwarding=1` — прокидывать все CGI-параметры из адреса страницы в ссылки и бесконечную ленту
* `&exp_flags=platform=touch` — принудительное открытие мобильного стаба данных в любом юзерагенте
* `&exp_flags=platform=desktop` — принудительное открытие десктопного стаба данных в любом юзерагенте
* `&platform=mobile` — принудительное открытие мобильной версии страницы
* `&noredirect=1` — отключение переадресации на страницу оригинала (например, в случае неподдерживаемого браузера или платформы)
* `&exp_flags=error_counter=1` — включение логирования ошибок
* `&exp_flags=fallback-disabled=1` — отключение фолбэка *(вне темплара)* (переадресации на оригинал при отсутствии данных)
* `&exp_flags=fallback-disabled=0` — включение фолбэка *(под темпларом)* (в режиме разработки по умолчанию [фолбек отключен](https://github.yandex-team.ru/serp/web4/blob/dev/.templar/plugins/testing-flags.js))
* `&exp_flags=validate_counters=1` — вывод информацию о баобаб-счетчиках
* `&nomooa=1` — отключить жука для баг-репортов

### Параметры только в Templar
* `&custom-css=/abs/path/to/style.min.css` — применяет кастомные стили из указанного файла *(только в режиме development)*
* `&save-custom-css=/abs/path/to/style.min.css` — сохраняет кастомные стили из указанного файла в стаб, указанный в параметре `&stub` *(только в режиме development)*
* `&ajaxstub=success` — для AJAX-запросов, вернуть успешный ответ
* `&ajaxstub=fail<5000>` — для AJAX-запросов, вернуть ошибку с задержкой в 5 секунд

### Features (предобработка корневых данных)

В директории `platform/features/{feature-name}/index.ts` можно располагать фичи. Фича — это набор правил трансформации входящего турбо-JSON'а от бекенда в новый турбо-JSON. Как правило это применимо для «тонких» страниц, где от бекенда приходит чистый набор данных, а нам на вёрстке нужно трансформировать его во view-ориентированный формат, где сформирована раскладка страницы с указанием конкретных компонентов. Далее на результирующий турбо-JSON применются адаптеры компонентов.

Фича может распространяться на весь документ целиком или на элемент с первого уровня вложенности.

**Фича на весь документ**

```json
{
    "platform": "touch-phone",
    "feature": "feature-name",
    "content": []
}
```

В этом случае первым параметром фичи будет весь документ:
```ts
import { IFeature } from '../../features/Feature';
import {
    IDoc,
    IBlock
} from '../../types/IData';

const rootFeature: IFeature<IDoc<IBlock>> = (doc) => {
    // Какие-то преобразования или отправка бандлов для страницы.

    return doc;
};

export default rootFeature;
```

**Фича на элемент с первого уровня вложенности**

```json
{
    "platform": "touch-phone",
    "content": [
        {
            "feature": "feature-name-1",
            "foo": "bar"
        },
        {
            "feature": "feature-name-1",
            "foo": "baz"
        }
    ]
}
```

В этом случае первым параметром фичи будет элемент, в котором указана фича:
```ts
import { IFeature } from '../../features/Feature';

const nestedFeature: IFeature<{ feature: string; foo: string; }> = (item) => {
    return {
        block: item.feature,
        content: item.foo
    };
};

export default nestedFeature;
```

### Переводы

Переводы платформы Турбо находятся в танкере ([ссылка](https://tanker.yandex-team.ru/?project=turbo&branch=master)).
Для синхронизации их с танкером, необходимо [добавить OAUTH token](https://wiki.yandex-team.ru/search-interfaces/tanker/tanker-kit/#oauth)
и выполнить команду `./node_modules/.bin/tanker sync`.

Лучше напрямую указать путь до блока, в котором используются переводы, иначе команда не выполнится. Например `./node_modules/.bin/tanker sync platform/components/Advert`.

Для работы с переводами используется [`@yandex-int/i18n`](https://github.yandex-team.ru/search-interfaces/frontend/blob/master/packages/i18n/README.md). [Пример](platform/components/Footer/Footer.adapter.tsx)

Для заказа переводов используйте [ссылку](https://wiki.yandex-team.ru/doc-and-loc/l10n/zakazatperevod/localizationrequest/)

### Прекоммитные проверки

На каждый коммит запускаются [несколько](https://github.yandex-team.ru/serp/turbo/tree/dev/.githooks/pre-commit) команд
для проверки нового кода, в том числе tsc для проверки тайпингов.
Она выполняется достаточно долго (около 30 сек.),
поэтому для ускорения создания коммита ее можно отключить переменной окружения `SKIP_TSC=1`.
Пример:

```bash
SKIP_TSC=1 git ci -m "Коммит без проверки тайпингов"
```

Как альтернатива выполнению проверки тайпингов перед коммитом,
можно запустить их проверку локально командой

```bash
npm run lint:tsc
```

## Версионирование разметки и стилей

**Для внутреннего использования сервисами Яндекса**

Мы предоставляем разработчикам возможность кастомизировать свои турбо-страницы с помощью произвольного CSS, который они могут матчить на большое число селекторов. Иногда мы вносим в наш HTML и CSS обратно несовместимые изменения, которые ломают стили внешних коллег. Чтобы минимизировать такой риск и обеспечить возможность плавного перехода на новые изменения сделан механизм версионирования.

Проблема в том, что кастомные стили лежат в одном месте — в вебмастере, но при этом должны работать по разному:
* в продакшене до выкатки новых шаблонов всё должно быть как раньше
* в разработке и тестировании, где уже новые шаблоны стили должны поменяться
* при раскатке релиза новые стили должны примениться в момент появления новых шаблонов (селекторы приходят на помощь, так как мы не можем обеспечить синхронную раскатку релиза шаблонов и заливку стилей в вебмастер)

Допустим, нам нужно обновить версию с `6` на `7`. Для этого:
1. В `package.json` изменяем поле: `"customcss-version": "7"`.
2. Версия появляется в атрибуте: `<body class="page" data-customcss-gte="1 2 3 4 5 6 7">`, где `gte` означает `>=`.
3. Кастомные стили можно класть каскадом от селектора по версии. Например, нижеприведённый селектор сработает при выкатке в продакшен версии шаблонов с обновлённой `7`-й версией в `package.json`:
```css
/** Матчимся на версию >= 7. */
.page[data-customcss-gte~="7"] .cover { ... }
```

## Дизайн-ревью
У нас в команде присутствуют процессы обязательных дизайн-ревью и очных ревью. Подробнее можно почитать по ссылке https://wiki.yandex-team.ru/turbo/design-review/

## Документация

### Hermione-тесты

Запуск тестов во время разработки:
```sh
NODE_ENV=development hermione gui (...) # или npm run hermione -- gui (...)
```

Запуск финального варианта тестов или отладка существующего теста (предварительно нужно собрать проект в testing, [см Разные среды запуска](#Разные-среды-запуска)):
```
NODE_ENV=testing hermione gui (...)
```

#### Включение рекламы и аналитики

Реклама является внешним ресурсом и часто мешает в тестах, потому что результаты начинают плавать. Счётчики систем аналитики так же являются внешними ресурсами, поэтому в тестах реклама и аналитика по умолчанию отключены.

* `&exp_flags=analytics-disabled=0` — включить аналитику (метрика и тд.)
* `&exp_flags=hermione_yanalytics=stub` — заменить [библиотеку yanalytics](https://github.yandex-team.ru/lp-constructor/yanalytics/) на стаб для тестирования
* `&exp_flags=adv-disabled=0` — включить рекламу
* `&exp_flags=adv-disabled=0&hermione_advert=stub` — показывать стабы рекламы
* `&exp_flags=adv-disabled=0&hermione_advert=fail` — имитировать незагруженную рекламу

#### Отключение дозагрузки страниц

Когда нужно снять всю страницу целиком или какой-то элемент ближе к концу первой страницы, чтобы тесты были стабильнее и не зависили от того, успеет загрузиться следующая страница или нет.

* `&hermione_autoload=disable` — отключить дозагрузку следующих страниц.

#### Отключение айфреймов

Айфрейм является внешним ресурсом, что замедляет выполнение тестов и негативно влияет на стабильность, поэтому часто лучше застабить айфрейм, если только тест не подразумевает тестирование содержимого айфрейма.

* `&hermione_videoplayer=stub` — показывать стабы песочницы видео-плеера

#### Отключение турбо-в-турбо

Для тестирования метрики нужно отключить переход по турбо-в-турбо ссылкам, это можно сделать
при помощи параметра `&hermione_turbo_in_turbo=disable`.

#### Стаб времени запроса
Нужно добавить query-параметр `user_time` в формате IETF RFC 2822 или ISO8601. Будет доступен в `ctx.data.reqdata.user_time.to_iso` в формате ISO8601
https://local.yandex.ru:3443/turbo?stub=preset-button%2Fbutton-data-from-settings.json&user_time=2018-05-15T04%3A36%3A21%2B0400

#### Ручки к темплару для тестов гермионы
Иногда необходимо выставлять в стабах ручку для темплара (который может запускаться на разных портах).
Это можно сделать, двумя способами:
1. Указать в качестве эндпоинта http://localhost:3333
2. Указать в качестве эндпоинта %%SCHEME%%://%%HOST%%
Пример: [sandwich-menu/with-auth.json](https://github.yandex-team.ru/serp/turbo/blob/dev/touch-phone.blocks/sandwich-menu/sandwich-menu.examples/withAuth.json)

#### Патчи для изменения данных

Если есть готовый дамп данных для теста, но в нём нужно точечно что-то поменять, то для этого существует механизм патчей.
Патчи располагаются по пути `report-cache/hermione/patch/*.js`. Каждый патч представляет из себя функцию, которая принимает на вход объект с данными всего дампа и мутирует его. Для использования патча достаточно добавить параметр с его именем в адресную строку: `&patch=changeTitle`.

```
// report-cache/hermione/patch/changeTitle.js

module.exports = function(ctx) {
    ctx.data.app_host.result.docs[0].title = 'Подменённый заголовок';
};
```
#### Переименование тестов
Для переименования можно использовать [tide-renamer](https://a.yandex-team.ru/arc/trunk/arcadia/frontend/packages/tide-renamer/README.md#%D0%B8%D1%81%D0%BF%D0%BE%D0%BB%D1%8C%D0%B7%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D0%B5-2)

#### Стабилизация тестов (погонятор, castle)

Стабильность теста определяется статистикой его запусков. Общую статистику запусков гермиона-тестов можно посмотреть через [testcop](https://testcop.si.yandex-team.ru) ([документация](https://github.yandex-team.ru/search-interfaces/infratest/tree/master/services/testcop)) — этот сервис служит для отключения и включения плавающих тестов.

Инструмент [castle](https://github.yandex-team.ru/search-interfaces/infratest/tree/master/packages/castle) позволяет запускать тесты в сэндбоксе без создания пулреквеста ([подробности в посте](https://clubs.at.yandex-team.ru/search-interfaces/2818)). С помощью castle мы можем принудительно запустить указанный тест много раз, чтобы узнать статистику его падений. Если тест часто падает, то его нужно стабилизировать.

Инструкция по запуску и просмотру отчёта прогона теста:
1. Запускаем с нужным набором параметров, аналогичных гермионе `npx castle run hermione --file [path/to/*.hermione.js] --grep "describe или it" --repeat 20 -p touch-phone`. Можно добавить параметр `--unskip TURBOUI-12345`, чтобы в прогоне расскипались тесты, заскипанные в указанном тикете.
2. После завершения выполнения команды в конце будут выведены [ссылки на задачи](https://jing.yandex-team.ru/files/tenorok/%D0%A1%D0%BD%D0%B8%D0%BC%D0%BE%D0%BA%20%D1%8D%D0%BA%D1%80%D0%B0%D0%BD%D0%B0%202020-07-03%20%D0%B2%2013.58.23.png) по запуску гермионы в сэндбоксе отдельно для каждой платформы
3. Открываем каждую задачу и [переходим в отчёт](https://jing.yandex-team.ru/files/tenorok/%D0%A1%D0%BD%D0%B8%D0%BC%D0%BE%D0%BA%20%D1%8D%D0%BA%D1%80%D0%B0%D0%BD%D0%B0%202020-07-03%20%D0%B2%2014.00.44.png), [смотрим на ретраи](https://jing.yandex-team.ru/files/tenorok/%D0%A1%D0%BD%D0%B8%D0%BC%D0%BE%D0%BA%20%D1%8D%D0%BA%D1%80%D0%B0%D0%BD%D0%B0%202020-07-03%20%D0%B2%2014.02.20.png) — в идеале все запуски должны быть зелёными

#### Лучшие практики

* Если что-то можно проверить без скриншота, то лучше не снимать скриншот (например, проверив текст на странице, проверив адрес вкладки браузера или проверив видимость элемента на странице).
* Важно, чтобы на скриншот не попадали лишние элементы, не относящиеся к данному конкретному тесту.
* Скриншот не должен содержать огромные пустые пространства — если нужно протестировать только конкретный блок, то указываем его селектор, а не всю страницу. Если нужно протестировать набор блоков, то их можно поместить в специальную обертку: `{ "block": "hermione-container", "content": [/* Блоки для тестирования. */] }`, при необходимости добавить ей стили: `"css": [".hermione-container { position: absolute; }"]` и использовать для скриншота `.assertView('plain', PO.hermioneContainer())`.
* Если нужно проверить эффект наведения на какую-то кнопку или иконку, то достаточно заскриншотить только её. При этом должен быть скриншот с дефолтным состоянием, где зафиксировано её расположение относительно соседних элементов.
* Если нужно проверить на скриншоте расположение элемента на странице важно заигнорировать все не относящиеся к проверке элементы: `.assertView('plain', PO.page(), { ignoreElements: ['.selector'] });` — на месте игнорируемых селекторов появятся чёрные квадраты.
* Всегда указывать сообщения об ошибках в методах, которые чего-то ожидают или проверяют, чтобы в отчёте было понятно что именно пошло не так.
* Наши кастомные команды (хелперы) называются с префикса `ya`. Если не хватает какой-то команды, то прежде, чем приступать к собственной реализации, нужно проверить существование подходящей команды в [web4](https://github.yandex-team.ru/serp/web4/tree/dev/hermione/commands/) и если она есть, скопировать её без изменений к нам с тем же именем.
* Для отладки Гермиону можно запустить с флагом `--debug`, тогда в отдельной вкладке браузера откроется удаленный рабочий стол с устройством и можно будет наблюдать все происходящее. При добавлении в тест команды `.debug()` выполнение будет приостанавливаться до того, как вы не продолжите его из консоли.
* Флаг `--debug-record-video filename` позволяет сохранить видео прохождения теста.
* Если в тесте нужно сменить ориентацию, то такой тест запускаем только в `chrome-phone` следующим образом: `hermione.only.in('chrome-phone', 'setOrientation() работает стабильно только в chrome-phone');`. При этом важно, что такой тест должен быть максимально атомарным, чтобы другие проверки выполнялись в остальных браузерах.
* Если в пулреквесте валидно упали проверки скриншотами и их нужно обновить, то можно воспользоваться этим отчетом из сэндбокса локально, чтобы не запускать эти тесты повторно: `hermione gui -f https://proxy.sandbox.yandex-team.ru/1044844271/index.html`.
* Для вывода логов с клиента можно использовать функцию `window.__hermioneLogger({...});` — отправленные данные будут выведены в консоль, где запущен темплар.

### Автоматизация работы с тестами
В проекте установлен инструмент [tide](https://clubs.at.yandex-team.ru/search-interfaces/4332), направленный на оптимизацию работы с тестами. Основные функции:

* Генерация testpalm.yml файлов из hermione-тестов. [Подробнее](https://clubs.at.yandex-team.ru/search-interfaces/4332).
  `npm run tide -- generate features/desktop/related/related.hermione.js`

* Переименование тестов с учётом зависимостей и переносом скриншотов и  дампов. [Подробнее](https://clubs.at.yandex-team.ru/search-interfaces/4356).
  `npm run tide -- rename features/desktop/related --old-name "Связанные на десктопе Внешний вид" --new-name "Связанные на десктопе Внутренний вид"`
  Указывать директорию теста не обязательно, но это ускоряет работу инструмента.

* Поиск дубликатов тестов. [Подробнее](https://clubs.at.yandex-team.ru/search-interfaces/4437).
  `npm run tide -- hunt`

## Данные
Все доступные данные можно посмотреть на http://localhost:3333

### Загрузка данных в SaaS

```
svn checkout svn+ssh://arcadia.yandex.ru/arc/trunk/arcadia/quality/functionality/turbo/tools/scripts/
cd scripts
cat test_news.json | ./upload_ampcache_json.py
```

В `test_news.json` находится массив из трёх наборов данных. Можно смело добавлять ещё, главное не забыть закоммитить это в Аркадию, иначе ваши загрузки кем-то перезапишутся.

HTML для этих статей можно посмотреть по адресу — https://yandex.ru/search/news-cache/touch?text=test_news&noredirect=1
JSON — https://yandex.ru/search/news-cache/touch?text=test_news&noredirect=1&export=json

#### Удаление данных из SaaS
В `delete_news.json` массив объектов с единственным значимым полем — `url`.
```
cd scripts
cat delete_news.json | ./delete_ampcache_json.py
```

Будьте аккуратны.

### Загрузка данных в SaaS способ 2
Существует еще веб-интерфейс, предоставленный @web-chib, находится по адресу `https://turbo-tools.yandex-team.ru/bins`

Чтобы создать новый документ достаточно нажать кнопку `new bin`. Key в редакторе будет соответствовать полю `text`.
Например, `yandex.ru/turbo?text=bin:test`
В JSON статьи должен быть следующего вида

```
{
  "content": [
    {
      "block": "a",
      "content": "b"
    }
  ]
}
```

Кнопки `Create bin` и `Update bin` - загружают документ в SaaS, справа можно увидеть статью.

Со всеми вопросами по этому сервису обращаться к @shubert

### Стабы

Для разработки достаточно положить JSON с данными:

#### Для BEMHTML-блоков:

в `common.blocks/page/page.examples/file.json`, чтобы посмотреть результат — http://localhost:3333/turbo?stub=page/file.json

#### Для React-блоков:

в `platform/components/Page/__tests__/examples/file.json`, чтобы посмотреть результат — http://localhost:3333/turbo?stub=page/file.json
Для написания теста под определенную платформу используется нейминг:

- file@desktop.json - для десктопа
- file@phone.json - для телефонов

### ExpFlags

Все флаги ```exp_flags``` хранятся в папке `./expflags`.
Создание флага по команде
```
 npm run flag:create
```

### Метрика

У нас есть тестовые счётчики метрики, они используются для тестов, чтобы не отправлять фейковые данные в реальные счётчики.
Для просмотра этих счётчиков нужно [запросить роль Суперсмотритель](https://wiki.yandex-team.ru/jandexmetrika/managerdostup/#supersmotritel) в [IDM](https://idm.yandex-team.ru).

<img src="https://jing.yandex-team.ru/files/tenorok/Snimok_ekrana_2018-12-17_v_13.04.49.png" width="500">

После этого должен открыться доступ к следующим тестовым счётчикам:
* https://metrika.yandex.ru/dashboard?id=45135375
* https://metrika.yandex.ru/dashboard?id=46417413

По умолчанию метрика отключена, и графики должны быть мёртвыми. Для включения аналитики и метрики в частности нужно добавить в адресную строку специальный параметр ([см. отдельный раздел](#Включение-рекламы-и-аналитики)).

## Соглашения
* не используем форки
* ветки именуются в формате `login.feature.SERP-1234`, `login.featureDescription`
    * обязательно указываем `login`, чтобы в дальнейшем найти владельца ветки
    * пишем краткое описание фичи camelCase после `login`
    * при наличии задачи в трекере указываем ID задачи
    * в качестве разделителя используем «.»
* PR содержит в себе либо «TRIVIAL: описание PR» либо «TASK-ID»
* в description PR хорошо писать человепонятное описание проделанной работы
* если в PR появляются новые или меняются существующие hermione-тесты,
  нужно запустить castle (или погонятор) для этих тестов,
  результат выполнения указать в поле в заголовке PR
* стараемся сквошить изменения до логических, атомарных коммитов, в противном случае — сквошим всё в один
    * в момент разработки — сколько угодно коммитов
    * сквошим перед сдачей задачи в ревью
    * изменения в локализации, дампов для тестов, скриншотов, хорошо отделять от кода, т.е. делать отдельными коммитами
* именуем коммиты следующим образом
    * шаблон текста коммита - `[ключ задачи]: [осмысленное описание того, что сделано]` (Пример: `TURBOUI-1234: Добавлен сниппет my-snippet`)
    * описание в тексте коммита пишется на русском языке
    * текст коммита должен описывать изменения в коде, а не работу разработчика (Пример: `TURBOUI-1234: Добавил новый сниппет` -> `TURBOUI-1234: Добавлен сниппет my-snippet`)
    * не копировать описание задачи в коммит сообщение. В задачах описывается что надо сделать, в коде что сделано
    * английские слова и обозначения лучше писать на английском, или же писать перевод (Пример: `прекоммит хуки` -> `TURBOUI-1234: Добавлены pre-commit hooks`)
* [Структура компонента в платформе](https://github.yandex-team.ru/serp/turbo/issues/3015#issuecomment-1283795)

## Ревью
* Перед запуском ревью кода, задачу надо отдать на ревью дизайнеру, и получить от него дизайн ок в тикете, после чего начинаем ревью кода. В случае, если в задаче не было дизайна, то дизайн ОК, не требуется.
* используем [ревьюшницу](https://wiki.yandex-team.ru/search-interfaces/review-faq/)
* два «/ok» от ревьюверов
* [Merge Queue](https://wiki.yandex-team.ru/search-interfaces/infra/infraspeed/docs/merge-queue/) **включён** — коммиты в `dev` напрямую запрещены, используйте `/merge` в PR.
 * https://mqs.si.yandex-team.ru/serp/turbo
 * https://mqs.si.yandex-team.ru/report/serp/turbo

### Полезные ссылки
* [приёмочные машины](https://wiki.yandex-team.ru/serp/serp-machines/#prijomkarelizov)
* [search priemka](https://wiki.yandex-team.ru/JandeksPoisk/Sepe/searchpriemka/)
* [howto](https://wiki.yandex-team.ru/users/sanity/howto/) от релиз-менеджера СЕРПа
* [Обработка событий](docs/TouchEvents.md)

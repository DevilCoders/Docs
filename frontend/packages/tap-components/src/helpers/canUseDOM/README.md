# canUseDOM

Проверяет, можно ли использовать браузерное API в текущем окружении.

## Пример использования

```typescript jsx
import { canUseDOM } from '@yandex-int/tap-components/helpers';

const isClientSide = canUseDOM();
```

# Поддерживаемые ОС

{{ serviceName }} устанавливается на серверах с операционными системами:

* linux и glibc 2.11+ (например, ubuntu lucid и старше);
* macOS.

{{ serviceName }} также может использоваться в среде Cygwin (на серверах).

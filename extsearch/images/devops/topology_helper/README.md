Usage:

```
./topology_helper \
    --nanny-oauth-file ~/.nanny_oauth_file \  # Nanny OAuth
    --config all_thumbs.json \  # Config
    --topology thumbs \  # Config section
    --release stable-131-r15  # Target topology
```

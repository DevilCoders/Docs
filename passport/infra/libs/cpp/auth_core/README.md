libyandex-auth-core
===================

Library for parsing Session_id cookies on server side

Session_id parsing and creation, keyspace management etc.
To build the package do `debuild` at root directory.

Docs available here: https://wiki.yandex-team.ru/passport/sessionid

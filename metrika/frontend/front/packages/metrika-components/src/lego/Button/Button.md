Button, theme: info

```(jsx)
const { Button } = require('./Button');

<Button theme="info" size="s">
    Кнопка
</Button>
```

Button, theme: update

```(jsx)
const { Button } = require('./Button');

<Button theme="update" size="s">
    Кнопка
</Button>
```

Button, theme: critical

```(jsx)
const { Button } = require('./Button');

<Button theme="critical" size="s">
    Кнопка
</Button>
```

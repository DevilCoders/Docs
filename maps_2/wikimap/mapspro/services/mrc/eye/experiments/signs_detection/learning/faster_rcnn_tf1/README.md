Граф обучения сети детектирования дорожных знаков в Нирване.

Для запуска необходимо выполнить:
```
ya make
./train_faster_rcnn run new_train
```

Чтобы использовать операции, которые еще не были опубликованы в Нирвану, необходимо выставить переменную окружения `VH3_DEV=1`.

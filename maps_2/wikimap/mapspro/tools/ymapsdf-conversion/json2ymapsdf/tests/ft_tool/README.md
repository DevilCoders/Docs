FT Tool
=======
The `FT Tool` starts a local PostgreSQL server, executes `json2ymapsdf` and
dumps exported data or prints configuration. After this, the output can be
checked against the canonized one.

For information about supported command line arguments, run the tool with
`--help` argument.

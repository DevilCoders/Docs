# morda backend 

Flask приложение фронтового бэкэнда автобус.

Запуск:

```
ya make && ./morda_backend gunicorn -b "unix:/tmp/bus_morda_backend.sock"
```

Для вывода всех опций:

```
./admin --help
```


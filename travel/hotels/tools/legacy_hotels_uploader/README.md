# Legacy hotel uploader

Загружает JSON индекс для legacy-ручки отелей в YT

## Как загрузить индекс?

Выполнить по порядку следующие команды:

```shell script
./download_resource.sh
ya make && ./legacy_hotels_uploader --output-table //home/travel/alexcrush/legacy-hotels
```

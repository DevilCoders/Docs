# Rubric mapping codegen

Генерирует маппинги к рубрикам Алтая для фидеров партнеров.

## Запуск генерации для всех партнеров

```shell script
ya make -t . && ./rubric_mapping_codegen
```

## Запуск для отдельного партнера
```shell script
ya make -t . && ./rubric_mapping_codegen [partner]
```

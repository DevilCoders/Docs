---
title: Выкатка сервиса заказов с помощью ПОНЯбот
---

## Выкатка сервиса заказов с помощью ПОНЯбот

### Testing
В testing выкатка происходит автоматом


### Prod
Перед выкаткой в prod необходимо прогнать acceptance тесты, и затем сделать релиз с помощью ПОНЯбот
1. Находим в sandbox сборку, которую хотим вывести в прод. Например, с помощью [интерфейса ПОНЯбот](https://ci.travel-dev.yandex-team.ru/components), где показываются последние сборки компонентов:
   - идем по ссылке в столбце Task напротив компонента "💳 Orders";
   - вкладка Resources;
   - копируесм id ресурса с типом TRAVEL_ORDERS_APP_BINARY.

    ![TRAVEL_ORDERS_APP_BINARY](_assets/Sandbox-TRAVEL_ORDERS_APP_BINARY.png "https://sandbox.yandex-team.ru/task/999872111/resources" =800x508)

2. Запускаем в sandbox новую задачу типа `TRAVEL_ORDERS_APP_ACCEPTANCE_RUNNER`:
    - открываем [пример таска](https://sandbox.yandex-team.ru/task/842193360/view);
    - делаем clone;
    - внизу страницы напротив `Orders app resource` в поле `Resource Id` вводим id, который нашли на 1-м шаге.
3. После успешного завершения acceptance тестов можно продолжать делать релиз. При неуспешном запуске идем и смотрим в лог, разбираемся что упало. В исключительных случаях (e.g. необходимо срочно катить хотфикс) допустимо, но крайне не рекомендуется, продолжать релиз с упавшим acceptance тестом, если разработчик понимает, что это падение вызвано недоступностью внешнего API партнера.
4. Проверить, есть ли новые миграции БД. Если есть, то накатить их по [инструкции](https://a.yandex-team.ru/arc/trunk/arcadia/travel/orders/tools/db_migrator/README.md).
5. Объявить о своём желании выкатить в прод в канале [#devops](https://yndx-travel.slack.com/archives/C1KL4016K) и запустить релиз собранного `TRAVEL_ORDERS_APP_BINARY` ресурса через интерфейс ПОНЯбота по ссылке из шага 1.


## Как откатить?
 [Инструкция](../../ops/tools/nanny.md#как-откатить-релиз).

## Кроссейловый блок отелей

Возвращает N популярных отелей в городе

Есть две версии ручки для получения данных

-   `GET /api/hotels_portal/v1/get_cross_sale_hotels` - возвращает минимальные цены
-   `GET /api/hotels_portal/v2/get_cross_sale_hotels` - возвращает реальные офферы

## Экстранет (ЛК отельера)

В Swagger методы вынесены в отдельную группу - `6. extranet`

## Методы

-   `getOrders` :: `POST /api/hotels_extranet/v1/get_orders`
-   `getBankOrders` :: `POST /api/hotels_extranet/v1/get_bank_orders`

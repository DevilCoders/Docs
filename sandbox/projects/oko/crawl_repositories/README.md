To compile and upload to Sandbox:
`ya make -r && ./crawl_repositories upload --attr task_type=OKO_CRAWLER`

To compile and run in the local Sandbox:
`ya make -r && ./crawl_repositories run --no-auth --skynet --url "http://localhost:8000" --create-only --type
OKO_CRAWLER`


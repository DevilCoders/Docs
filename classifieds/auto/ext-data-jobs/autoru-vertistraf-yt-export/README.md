# Vertistraf YT export

Job that exports searcher data to YT for VERTISTRAF team needs.

## Local development

### Config

To develop locally it is recommended to replace `application.conf` with the content of `application.example.conf`.

# MemStore service implementation

MemStore is a lightweight in-memory storage for time series and their metadata.

## Directories structure

```
.
├── api                 - public gRPC API schema
├── bin                 - main binary code
├── config              - config file schema
└── lib                 - non public libraries (must be used only in memstore code)
```

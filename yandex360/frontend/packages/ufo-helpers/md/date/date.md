## Functions

<dl>
<dt><a href="#getDaysAgoDate">getDaysAgoDate(date, days)</a> ⇒ <code>Date</code></dt>
<dd><p>возвращает новую дату, которая была <code>days</code> дней назад относительно <code>date</code></p>
</dd>
<dt><a href="#wasItYesterday">wasItYesterday(date, [now])</a> ⇒ <code>boolean</code></dt>
<dd><p>Проверяет была ли одна дата относительно другой «вчера»</p>
</dd>
</dl>

<a name="getDaysAgoDate"></a>

## getDaysAgoDate(date, days) ⇒ <code>Date</code>
возвращает новую дату, которая была `days` дней назад относительно `date`

**Kind**: global function  

| Param | Type |
| --- | --- |
| date | <code>Date</code> | 
| days | <code>number</code> | 

<a name="wasItYesterday"></a>

## wasItYesterday(date, [now]) ⇒ <code>boolean</code>
Проверяет была ли одна дата относительно другой «вчера»

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| date | <code>Date</code> |  |
| [now] | <code>Date</code> | "текущий" момент времени |


```sh
# Generate SSL certificate for the local server
# Run once after cloning the repo
yarn mkcert

# Start the dev server
yarn dev

# Or build a production bundle and run the preview
yarn build
yarn preview

# Visit https://demo.localhost
```

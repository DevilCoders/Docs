Конфиги разбиты на модули по смыслу.

Каждый модуль экспортирует объект `config` с полями по именам всех окружений.

Список окружений находится в модуле [common.ts](./common.ts) в переменной `APP_ENVIRONMENTS`.

Для примера можно смотреть в модуль [logger.ts](./logger.ts).

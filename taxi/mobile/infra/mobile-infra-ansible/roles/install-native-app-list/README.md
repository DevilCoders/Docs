Install native app
----
Role for install native app

Parameters
----

```yaml
native_app_info_list:
  - install_command: string|required
  - install_command: string|required
```

* **bin_path_env*** (string|required) - env for brew path. For example /opt/homebrew/bin:/opt/homebrew/sbin:
  /usr/local/bin).
Пояснения по Докеру

Чтобы сбилдить образ используем скрипт build.sh. Для билда требуется переменная окружения OAUTH_TOKEN.

Для запуска используем run.sh. Он никаких зависимостей внешних не требует.

Скрипт, который объеденяет сразу 2, - deploy.sh. Он одновременно и билдит и запускает образ

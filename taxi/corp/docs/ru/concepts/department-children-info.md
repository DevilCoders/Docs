# Получение списка дочерних подразделений

Запрос позволяет получить информацию о дочерних подразделениях.

## Синтаксис запроса

```
GET https://business.taxi.yandex.ru/api/1.0/client/{идентификатор клиента}/departament/{идентификатор подразделения}/children
```

{% include [order-create-headers-p](../_includes/concepts/order-create/id-order-create/headers-p.md) %}


#### Authorization
OAuth-токен. Процесс получения токена описан в разделе [Начало работы](quickstart.md).

## Описание полей ответа

В ответе могут содержаться следующие поля:

Поле | Описание | Формат
----- | ----- | -----
`departments[]` | Список подразделений. | Строка
`departments[]._id` | Идентификатор подразделения. | Строка
`departments[].name` | Название подразделения. | Строка
`departments[].parent_id` | Идентификатор вышестоящего подразделения. | Строка


## Пример запроса

```
GET https://business.taxi.yandex.ru/api/1.0/client/233e7...8fd7/departament/2f...071f34c53/children
...Authorization: <OAuth-токен>
```

## Пример ответа

Пример ответа на данный запрос выглядит следующим образом:

```no-highlight
{
  "departments": [
    { 
            "id": "e26c430a0e3a4d3eb639987d085837e6",
            "name": "Department 1",
            "parent_id": "2f...071f34c53"
    },
    {
            "id": "cf7130024b034f548aa6c42bfa810efe",
            "name": "Department 2",
            "parent_id": "2f...071f34c53"

    }
  ] 
}
```

## Возможные коды ответа

Ответ на данный запрос может содержать следующие стандартные HTTP-коды:

- `200` — запрос выполнен успешно.
- `401` — был передан неверный [OAuth-токен](quickstart.md).
- `403` — у клиента не хватает прав на выполнение данного запроса.
- `404` — запрашиваемая запись не найдена.


# Информация о клиенте

Запрос позволяет получить базовую информацию о клиенте.

## Синтаксис запроса

```
GET https://business.taxi.yandex.ru/api/auth
```

Заголовки запроса:

#### Authorization
OAuth-токен. Процесс получения токена описан в разделе [Начало работы](quickstart.md).

## Описание полей ответа

В ответе могут содержаться следующие поля:

Поле | Описание | Формат
----- | ----- | -----
`yandex_login` | Имя учетной записи на Яндексе. | Строка
`role` | Роль в личном кабинете. Для клиента возвращается со значением `client`. | Строка
`client_id` | Идентификационный номер контракта клиента. | Строка


## Пример запроса

```
GET https://business.taxi.yandex.ru/api/auth
...
Authorization: <OAuth-токен>

```

## Пример ответа

Пример ответа на данный запрос выглядит следующим образом:

```no-highlight
{
  "yandex_login": "example-company",
  "role": "client",
  "client_id": "a2...d09"
}
```

## Возможные коды ответа

Ответ на данный запрос может содержать следующие стандартные HTTP-коды:

- `200` — запрос выполнен успешно.
- `401` — клиент не авторизован.


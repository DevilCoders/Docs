# yav клиент
клиент для API секретницы

### Параметры клиента:

**_token_** - oAuth токен для API секретницы (по умолчанию берем из переменной окружения YAV_AUTH_TOKEN)

**_url_** - путь до API секретницы (по умолчанию берем из параметра конфигурации yav.url)

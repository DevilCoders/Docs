В обеих ручках в корни запросов добавляем:
```
{
    "preorder_request_id": "...", # id из preorder/v1/availability
    ...
}
```

Пока бек ручек будет ходить в ml для проверки due.
preorder_request_id понадобится на случая если мы будем сохранять обещания
пассажиру по времени (как оффер).

### Выставление дефолтных весов

`vertical`.yaml — возвращает в дефолт веса вертикали `vertical`.

all.yaml — возвращает в дефолт все вертикали.

С помощью параметра `vertical`_`location` можно отдельно задавать вес
для определенной вертикали в определенной локации.

# sleep - подождать указанное время

Использование:

```
# спать ровно 90 секунд, потом вернуться на исполнение
- name: sleep for 90 seconds
  sleep:
    duration: 90
```

Джоба остаётся в состоянии `running`.

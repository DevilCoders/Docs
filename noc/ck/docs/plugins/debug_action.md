# debug - отладочное сообщение

Дает возможность вывести в лог сообщение, позволяет использовать jinja-шаблоны, чтобы раскрывать переменные.

Пример - выводит в лог строку "Value of foo is 1"

```yaml
  - set_fact:
      foo: 1
  - debug:
      msg: "Value of foo is {foo}"
```

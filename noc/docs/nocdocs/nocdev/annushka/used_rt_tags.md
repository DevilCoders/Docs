# Список используюемых тегов RT в генераторах

В данной статье приведен список используемых в генераторах Аннушки тегов RT.

## aaa.py

В генераторе `aaa.py` используются следующие теги:
- Juniper MX
- P-маршрутизатор
- StoreData
- WiFi контроллер
- WiFi точка
- Владимир
- Ивантеевка
- ММТС10
- Морозов лаборатория
- Мытищи
- Мянтсяля
- пиринговый свитч
- Сасово
- свитч ABB
- свитч для IPMI
- юзерский свитч

## bandwidth.py

В этом генераторе не используется ни один из тегов RT.

## bfd.py

В генераторе `bfd.py` используются следующие теги:
- border router
- datacenter edge
- P-маршрутизатор
- with bfd
- магистральный свитч

## bgp.py

В генераторе `bgp.py` используются следующие теги:
- blackhole
- route reflector
- spine1
- spine2
- ToR-агрегатор slave
- многодэшка
- нагрузка снята
- Сасово-1

## bmp.py

В этом генераторе не используется ни один из тегов RT.

## border_cust.py

В этом генераторе не используется ни один из тегов RT.

## bridge.py

В этом генераторе не используется ни один из тегов RT.

## capsman.py

В этом генераторе не используется ни один из тегов RT.

## commit.py

В генераторе `commit.py` используются следующие теги:
- double RE

## community.py

В этом генераторе не используется ни один из тегов RT.

## copp.py

В генераторе `copp.py` используются следующие теги:
- backbone core
- border router
- datacenter edge
- L3 ToR
- P-маршрутизатор
- Ивантеевка
- многодэшка
- серверный свитч

## data_services.py

В этом генераторе не используется ни один из тегов RT.

## descriptions.py

В этом генераторе не используется ни один из тегов RT.

## dns.py

В генераторе `dns.py` используются следующие теги:
- многодэшка

## dot1x.py

В генераторе `dot1x.py` используются следующие теги:
- юзерский свитч

## dummy.py

В этом генераторе не используется ни один из тегов RT.

## firewall.py

В генераторе `firewall.py` используются следующие теги:
- ADD-PATH reflector
- border router
- cloud edge
- datacenter edge
- datacenter fabric
- decap_mpls_over_udp
- firewall
- IX
- MPLS BACKBONE
- P-маршрутизатор
- route reflector
- tunnel endpoint
- многодэшка
- пиринговая сеть
- свитч агрегации

## forwarding_options.py

В генераторе `forwarding_options.py` используются следующие теги:
- ADD-PATH reflector
- border router
- chassis
- cloud edge
- datacenter edge
- P-маршрутизатор
- route reflector
- spine1
- многодэшка

## hostname.py

В этом генераторе не используется ни один из тегов RT.

## huawei_common.py

В этом генераторе не используется ни один из тегов RT.

## hw_resources.py

В генераторе `hw_resources.py` используются следующие теги:
- L3 ToR
- MC-lagg device
- spine1
- spine2
- Мытищи
- пиринговый свитч
- Сасово
- Сасово-2
- свитч агрегации
- серверный свитч

## icmp.py

В этом генераторе не используется ни один из тегов RT.

## ifstat.py

В этом генераторе не используется ни один из тегов RT.

## isis.py

В генераторе `isis.py` используются следующие теги:
- fastbone
- MPLS FASTBONE
- лаборатория Владимир

## l2vpn.py

В этом генераторе не используется ни один из тегов RT.

## l3_ifaces.py

В генераторе `l3_ifaces.py` используются следующие теги:
- IX
- L3 ToR
- MPLS BACKBONE
- mpls enabled
- MPLS FASTBONE
- spine1
- Мытищи
- серверный свитч

## l3tor.py

В генераторе `l3tor.py` используются следующие теги:
- fastbone
- mtn shift
- no Autonomous flag
- NO RA
- Other flag
- spine1
- многодэшка
- неисполненный конфиг

## lagg.py

В генераторе `lagg.py` используются следующие теги:
- datacenter edge
- P-маршрутизатор
- spine1
- spine2
- Сасово-2.5
- Сасово-2.8
- Сасово-2.9

## license.py

В этом генераторе не используется ни один из тегов RT.

## lldp.py

В генераторе `lldp.py` используются следующие теги:
- пиринговая сеть
- пиринговый свитч
- серверный свитч
- чужая сеть

## load_balance.py

В генераторе `load_balance.py` используются следующие теги:
- fastbone core
- FL enabled
- spine1
- spine2
- Владимир
- многодэшка
- Мытищи
- Мянтсяля
- Сасово-1
- Сасово-2
- серверный свитч

## local_users.py

В генераторе `local_users.py` используются следующие теги:
- cloud edge
- datacenter fabric
- неисполненный конфиг

## mclagg.py

В генераторе `mclagg.py` используются следующие теги:
- MC-lagg device
- MC-lagg master

## mgmt.py

В генераторе `mgmt.py` используются следующие теги:
- environment

## mgmt_acl.py

В генераторе `mgmt_acl.py` используются следующие теги:
- многодэшка

## mpls.py

В генераторе `mpls.py` используются следующие теги:
- dc-ix
- fastbone core
- MPLS BACKBONE
- MPLS FASTBONE
- mpls_te enabled
- route reflector
- многодэшка
- пиринговая сеть
- чужая сеть

## mpls_te.py

В генераторе `mpls_te.py` используются следующие теги:
- mtu 9000
- многодэшка

## mtu.py

В генераторе `mtu.py` используются следующие теги:
- MPLS BACKBONE
- MPLS FASTBONE
- mtu 1500
- mtu 9000
- пиринговая сеть
- управляющая сеть

## myt6d.py

В генераторе `myt6d.py` используются следующие теги:
- L3 ToR

## ntp.py

В генераторе `ntp.py` используются следующие теги:
- Финляндия

## ospf.py

В генераторе `ospf.py` используются следующие теги:
- fastbone
- MPLS BACKBONE
- mtu 9000

## peerings.py

В генераторе `peerings.py` используются следующие теги:
- Европа
- ММТС9
- нагрузка снята
- Россия
- США

## physical_ifaces.py

В генераторе `physical_ifaces.py` используются следующие теги:
- SFP+

## plists.py

В этом генераторе не используется ни один из тегов RT.

## policy.py

В генераторе `policy.py` используются следующие теги:
- нагрузка снята

## portmode.py

В генераторе `portmode.py` используются следующие теги:
- portmode 10G
- серверный свитч

## project_id_acl.py

В генераторе `project_id_acl.py` используются следующие теги:
- Ивантеевка
- Мытищи
- Сасово

## qos.py

В генераторе `qos.py` используются следующие теги:
- cloud_forwarder
- datacenter edge
- fw-ix
- Juniper MX
- L3 ToR
- MPLS BACKBONE
- spine1
- spine2
- Владимир
- ММТС9
- Мытищи
- Мянтсяля
- пиринговый свитч
- Сасово
- Сасово-2
- серверный свитч

## routing_instances.py

В генераторе `routing_instances.py` используются следующие теги:
- border router
- Cloud
- cloud border
- многодэшка

## sampler.py

В генераторе `sampler.py` используются следующие теги:
- chassis
- cloud edge
- datacenter edge
- P-маршрутизатор
- Ивантеевка
- конюшня Cloud

## scheduler.py

В генераторе `scheduler.py` используются следующие теги:
- static_ND
- серверный свитч

## services.py

В генераторе `services.py` используются следующие теги:
- многодэшка

## sflow.py

В генераторе `sflow.py` используются следующие теги:
- cloud edge
- collector
- datacenter edge
- P-маршрутизатор
- конюшня Cloud

## shutdown.py

В этом генераторе не используется ни один из тегов RT.

## snmp.py

В генераторе `snmp.py` используются следующие теги:
- Cisco IE-2000

## snmp_acl.py

В этом генераторе не используется ни один из тегов RT.

## snmp_trap.py

В генераторе `snmp_trap.py` используются следующие теги:
- Cisco IE-2000
- syslog
- серверный свитч

## split.py

В генераторе `split.py` используются следующие теги:
- chassis
- StoreData
- Ивантеевка
- Марьина Роща 40
- ММТС9
- Сасово-2.11
- Сасово-2.13
- свитч агрегации
- серверный свитч

## static.py

В генераторе `static.py` используются следующие теги:
- blackhole
- ToR-агрегатор
- ToR-агрегатор slave
- Сасово-1

## storm_control.py

В генераторе `storm_control.py` используются следующие теги:
- свитч

## stp.py

В генераторе `stp.py` используются следующие теги:
- border router
- distrubution с FastBone
- MC-lagg device
- spine1
- spine2
- StoreData
- агрегатор IPMI
- Амстердам
- Владимир
- Ивантеевка
- Марьина Роща 40
- ММТС9
- многодэшка
- Мытищи
- Мянтсяля
- пиринговый свитч
- Сасово-1
- Сасово-1 КЦ
- свитч агрегации
- свитч для IPMI
- серверный свитч

## syslog.py

В генераторе `syslog.py` используются следующие теги:
- chassis
- syslog

## tcam.py

В этом генераторе не используется ни один из тегов RT.

## telemetry.py

В этом генераторе не используется ни один из тегов RT.

## trill.py

В этом генераторе не используется ни один из тегов RT.

## vlans.py

В генераторе `vlans.py` используются следующие теги:
- L3 ToR
- MC-lagg device
- T
- свитч агрегации
- юзерский свитч

## vrrp.py

В генераторе `vrrp.py` используются следующие теги:
- многодэшка

## vty.py

В этом генераторе не используется ни один из тегов RT.

## yattl.py

В генераторе `yattl.py` используются следующие теги:
- datacenter fabric
- firewall
- L3 ToR
- spine1
- spine2
- Владимир КЦ
- Владимир-1
- ММТС9
- многодэшка
- Сасово-2
